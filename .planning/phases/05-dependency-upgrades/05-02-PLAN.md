---
phase: 05-dependency-upgrades
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - SwtorLogParser.Native.Cli/SwtorLogParser.Native.Cli.csproj
  - SwtorLogParser.Native.Cli/Program.cs
  - SwtorLogParser.Cli/SwtorLogParser.Cli.csproj
  - SwtorLogParser.Cli/Program.cs
autonomous: false
requirements: [DEP-03, INFRA-02]
must_haves:
  truths:
    - "Neither CLI host references System.CommandLine or System.CommandLine.Rendering (in csproj or in any using/code)"
    - "Both hosts dispatch list/monitor via a hand-rolled switch on args[0]; unknown arg prints usage and exits non-zero"
    - "monitor still stops cleanly on Ctrl+C in BOTH hosts via Console.CancelKeyPress -> CancellationTokenSource feeding CombatLogsMonitor.Instance.Start(token) and .Stop()"
    - "The managed CLI renders the live 5-column table (Player, dps, (crit %), hps, (crit %)) via Spectre.Console with N format and '-' for nulls"
    - "The Native AOT CLI keeps its PERF-02 in-place cursor renderer unchanged and does NOT reference Spectre.Console"
    - "DockerDefaultTargetOS=Linux is absent from every csproj in the repo"
    - "dotnet build SwtorLogParser.slnx succeeds; dotnet test is GREEN with 106 passing, zero skips"
    - "dotnet publish SwtorLogParser.Native.Cli -c Release produces an AOT binary with no new trim/AOT warnings"
  artifacts:
    - path: "SwtorLogParser.Cli/Program.cs"
      provides: "Hand-rolled dispatch + Spectre.Console live table + Ctrl+C bridge"
      contains: "Spectre.Console"
    - path: "SwtorLogParser.Native.Cli/Program.cs"
      provides: "Hand-rolled dispatch + Ctrl+C bridge; PERF-02 renderer preserved"
      contains: "Console.CancelKeyPress"
    - path: "SwtorLogParser.Cli/SwtorLogParser.Cli.csproj"
      provides: "Spectre.Console ref (managed CLI only); no System.CommandLine.Rendering; no Docker prop"
      contains: "Spectre.Console"
  key_links:
    - from: "Ctrl+C (Console.CancelKeyPress)"
      to: "CombatLogsMonitor.Instance.Stop()"
      via: "CancellationTokenSource.Cancel() -> token.WaitHandle.WaitOne() unblocks -> Stop()"
      pattern: "Console\\.CancelKeyPress"
    - from: "SwtorLogParser.Cli Update()"
      to: "Spectre.Console Table"
      via: "AnsiConsole.Write(table) with 5 AddColumn/AddRow"
      pattern: "AnsiConsole\\.Write"
---

<objective>
Remove `System.CommandLine` (beta) and `System.CommandLine.Rendering` (alpha) from BOTH CLI hosts (DEP-03), replace the `RootCommand`/`Command`/`SetHandler`/`InvokeAsync` setup with a hand-rolled `switch (args[0])` dispatcher in each `Program.cs`, re-wire Ctrl+C cancellation through `Console.CancelKeyPress` → `CancellationTokenSource` (the framework previously supplied this token), port the managed CLI's 5-column `TableView` to a Spectre.Console `Table`, keep the Native AOT host's PERF-02 cursor renderer untouched and Spectre-free, and remove `DockerDefaultTargetOS=Linux` from both CLI csproj (INFRA-02).

Purpose: Eliminate the last preview/alpha/beta dependencies and the abandoned rendering alpha, keeping every host's behavior identical and the Native CLI AOT-publishable. Runs in Wave 2 because it re-edits the two CLI csproj that plan 05-01 already touched (CPM migration), so it cannot run in parallel with 05-01.
Output: Two rewritten `Program.cs` files; `Spectre.Console` referenced only by the managed CLI csproj; `System.CommandLine*` and the Docker property gone.
</objective>

<execution_context>
@D:/Projects/pjmagee/swtor-logparser/.claude/gsd-core/workflows/execute-plan.md
@D:/Projects/pjmagee/swtor-logparser/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dependency-upgrades/05-RESEARCH.md
@.planning/phases/05-dependency-upgrades/05-PATTERNS.md
@.planning/phases/05-dependency-upgrades/05-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- Spectre.Console `Table` live rendering in the managed CLI (`SwtorLogParser.Cli/Program.cs`) — this plan.
- `Directory.Packages.props` — produced by plan 05-01 (already exists when this plan runs); already declares `<PackageVersion Include="Spectre.Console" Version="0.57.0" />` so this plan only adds a BARE `<PackageReference Include="Spectre.Console" />` to the managed CLI csproj.
</artifacts_this_phase_produces>

<read_first>
- `SwtorLogParser.Native.Cli/Program.cs` — the analog AND a target. Current: `async Task<int> Main` with RootCommand/Command/SetHandler/InvokeAsync; `MonitorCombatLogs(InvocationContext context)` gets its token from `context.GetCancellationToken()` and pins a `ManualResetEvent` via `SetSafeWaitHandle(token.WaitHandle.SafeWaitHandle)` then `WaitOne()`. Everything from `_lastRowCount` / `Update(...)` / `FormatRow(...)` / `OnCombatLogAdded(...)` / `ListCombatLogs()` down is the PERF-02 renderer and has NO System.CommandLine dependency — keep it byte-identical (do-not-break #6).
- `SwtorLogParser.Cli/Program.cs` — current: same RootCommand setup; `MonitorCombatLogs(InvocationContext context)` uses `context.GetCancellationToken()`, `context.Console.GetTerminal()`, `terminal.HideCursor()`, and a `ConsoleRenderer(context.Console, OutputMode.Ansi)`; `Update(ConsoleRenderer, PlayerStats)` builds a `TableView<PlayerStats>` with 5 columns and calls `tableView.Render(renderer, Region)`. The `private static readonly Region Region = new(0, 0);` field is System.CommandLine.Rendering. `OnCombatLogAdded` and `ListCombatLogs` have no framework dependency.
- `SwtorLogParser.Cli/SwtorLogParser.Cli.csproj` — has `System.CommandLine.Rendering` ref (with Version=, left intact by 05-01) and `<DockerDefaultTargetOS>Linux</DockerDefaultTargetOS>` (line 9). `<PublishAot>false</PublishAot>` stays.
- `SwtorLogParser.Native.Cli/SwtorLogParser.Native.Cli.csproj` — has bare `System.CommandLine` ref (after 05-01) and `<DockerDefaultTargetOS>Linux</DockerDefaultTargetOS>` (line 8). `<PublishAot>true</PublishAot>` MUST stay (do-not-break #2).
- 05-PATTERNS.md "DO-NOT-BREAK LIST" — the 10 invariants. Honor every one.
- 05-RESEARCH.md "Code Examples" — canonical per-host dispatch + cancellation-bridge + Spectre Table patterns.
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Native AOT CLI — hand-rolled dispatch + Ctrl+C bridge, drop System.CommandLine + Docker prop</name>
  <files>SwtorLogParser.Native.Cli/Program.cs, SwtorLogParser.Native.Cli/SwtorLogParser.Native.Cli.csproj</files>
  <action>
DEP-03 + INFRA-02 for the Native AOT host. Do the csproj edit and the Program.cs edit together so the project compiles after.

csproj (`SwtorLogParser.Native.Cli.csproj`):
- DELETE the `<PackageReference Include="System.CommandLine" />` line entirely (no GA path was wanted; hand-rolled dispatch replaces it).
- DELETE the `<DockerDefaultTargetOS>Linux</DockerDefaultTargetOS>` property line (INFRA-02 — no Dockerfile exists; Windows-only app).
- Do NOT touch `<PublishAot>true</PublishAot>` or the bare `Microsoft.Extensions.Logging.Abstractions` ref (do-not-break #2). Do NOT add Spectre.Console here (do-not-break #2 / Pitfall 2 — Spectre stays out of the AOT host).

Program.cs (`SwtorLogParser.Native.Cli/Program.cs`):
- Remove the `using System.CommandLine;` and `using System.CommandLine.Invocation;` lines.
- Replace `Main` and `MonitorCombatLogs` so there is no System.CommandLine type anywhere. New `Main` signature: `public static int Main(string[] args)` — a `switch` on `args.Length > 0 ? args[0] : ""` routing `"list"` → `ListCombatLogs()` (return 0), `"monitor"` → `MonitorCombatLogs()` (return 0), default → write `Usage: SwtorLogParser.Native.Cli [list|monitor]` to `Console.Error` and return 1 (V5 input-validation allow-list, per 05-RESEARCH Security Domain).
- Rewrite `MonitorCombatLogs` to take NO parameter. Inside: `using var cts = new CancellationTokenSource();` then `Console.CancelKeyPress += (_, e) => { e.Cancel = true; cts.Cancel(); };` (set `e.Cancel = true` so the process is not killed before clean Stop — Anti-pattern + Pitfall 3). Use `var token = cts.Token;`. PRESERVE the existing blocking pattern: keep `using var manualResetEvent = new ManualResetEvent(false);` and `manualResetEvent.SetSafeWaitHandle(token.WaitHandle.SafeWaitHandle);` then `manualResetEvent.WaitOne();` (this is the exact mechanic the old code used — only the token SOURCE changes from `context.GetCancellationToken()` to `cts.Token`). After `WaitOne()` returns, call `CombatLogsMonitor.Instance.Stop();` for an explicit clean wind-down. Keep the body's `var list = new SlidingExpirationList(TimeSpan.FromSeconds(30));`, the `CombatLogAdded += OnCombatLogAdded`, the `DpsHps.Subscribe(playerStats => Update(list, playerStats))`, and `Start(token)` exactly as before.
- Leave `_lastRowCount`, `Update`, `FormatRow`, `OnCombatLogAdded`, and `ListCombatLogs` UNCHANGED (do-not-break #6 — the PERF-02 in-place renderer + IsOutputRedirected guards must remain byte-identical; only the command/cancellation plumbing changes).

Then build the project: `dotnet build SwtorLogParser.Native.Cli/SwtorLogParser.Native.Cli.csproj`.
  </action>
  <verify>
    <automated>dotnet build SwtorLogParser.Native.Cli/SwtorLogParser.Native.Cli.csproj succeeds (exit 0). Program.cs contains "Console.CancelKeyPress" and a switch over args, contains NO "System.CommandLine" and NO "InvocationContext". The csproj contains NO "System.CommandLine", NO "DockerDefaultTargetOS", NO "Spectre.Console", and still contains "<PublishAot>true".</automated>
  </verify>
  <done>Native CLI builds; dispatches list/monitor via switch; Ctrl+C bridge wired through CancellationTokenSource; PERF-02 renderer untouched; System.CommandLine + Docker prop removed; AOT property + Spectre-free invariant preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Managed CLI — hand-rolled dispatch + Spectre.Console table + Ctrl+C bridge, drop System.CommandLine.Rendering + Docker prop</name>
  <files>SwtorLogParser.Cli/SwtorLogParser.Cli.csproj, SwtorLogParser.Cli/Program.cs</files>
  <action>
DEP-03 + INFRA-02 for the managed host.

csproj (`SwtorLogParser.Cli.csproj`):
- DELETE the `<PackageReference Include="System.CommandLine.Rendering" ... />` line entirely (abandoned alpha — replaced by Spectre.Console).
- ADD a BARE `<PackageReference Include="Spectre.Console" />` (no Version= — it resolves from `Directory.Packages.props` where 05-01 declared `Spectre.Console` 0.57.0). Spectre goes ONLY here, never in the Native CLI (do-not-break #2).
- DELETE the `<DockerDefaultTargetOS>Linux</DockerDefaultTargetOS>` line (INFRA-02).
- Leave `<PublishAot>false</PublishAot>`, `<LangVersion>preview</LangVersion>`, and the ProjectReference unchanged.

Program.cs (`SwtorLogParser.Cli/Program.cs`):
- Remove all `using System.CommandLine*;` lines (`System.CommandLine`, `System.CommandLine.Invocation`, `System.CommandLine.Rendering`, `System.CommandLine.Rendering.Views`). ADD `using Spectre.Console;`.
- DELETE the `private static readonly Region Region = new(0, 0);` field (System.CommandLine.Rendering type). KEEP `private static readonly SlidingExpirationList List = new(TimeSpan.FromSeconds(30));`.
- Replace `Main` with `public static int Main(string[] args)` using the SAME switch shape as the Native host: `"list"` → `ListCombatLogs(); return 0;`, `"monitor"` → `MonitorCombatLogs(); return 0;`, default → `Console.Error.WriteLine("Usage: SwtorLogParser.Cli [list|monitor]"); return 1;`.
- Rewrite `MonitorCombatLogs` to take NO parameter and drop ALL System.CommandLine(.Rendering) members: remove `context.GetCancellationToken()`, `context.Console.GetTerminal()`, `terminal.HideCursor()`, and the `ConsoleRenderer(context.Console, OutputMode.Ansi)`. Wire the SAME Ctrl+C bridge as Task 1: `using var cts = new CancellationTokenSource(); Console.CancelKeyPress += (_, e) => { e.Cancel = true; cts.Cancel(); };`. Replace cursor-hide with `Console.CursorVisible = false;`. Keep `CombatLogAdded += OnCombatLogAdded;` and subscribe `CombatLogsMonitor.Instance.DpsHps.Subscribe(Update)` (Update now takes only `PlayerStats` — see below). Call `CombatLogsMonitor.Instance.Start(cts.Token);` then block on `cts.Token.WaitHandle.WaitOne();` and finally `CombatLogsMonitor.Instance.Stop();`.
- Rewrite `Update` to signature `private static void Update(CombatLogsMonitor.PlayerStats playerStats)` (no `ConsoleRenderer` param). Body: `List.AddOrUpdate(playerStats);` then build a Spectre `Table` and reproduce the EXACT 5 columns in order (do-not-break #5 — DO NOT change order, headers, the `"N"` format, or `"-"` for nulls):
  1. header `"Player"`   → cell `x.Player.Name ?? "-"`
  2. header `"dps"`      → `x.DPS.HasValue ? x.DPS.Value.ToString("N") : "-"`
  3. header `"(crit %)"` → `x.DPSCritP.HasValue ? x.DPSCritP.Value.ToString("N") : "-"`
  4. header `"hps"`      → `x.HPS.HasValue ? x.HPS.Value.ToString("N") : "-"`
  5. header `"(crit %)"` → `x.HPSCritP.HasValue ? x.HPSCritP.Value.ToString("N") : "-"`
  Add the five columns via `AddColumn`, iterate `List.Items` adding rows via `AddRow(...)`, then refresh the screen: `AnsiConsole.Clear(); AnsiConsole.Write(table);` (full re-render matches the old fixed-Region(0,0) behavior — behavioral parity). Using `AnsiConsole.Live(table)` for flicker-free updates is allowed at discretion but is NOT required; parity-first `Clear()+Write` is the default.
- Rewrite `OnCombatLogAdded` to not use System.CommandLine types: it currently does manual SetCursorPosition + clear + write of `combatLog.FileInfo`. Keep it simple and behavior-equivalent — either keep the existing `Console.SetCursorPosition`/`Console.Write` lines (they are plain BCL, no framework dependency) OR emit the filename via `AnsiConsole.MarkupLineInterpolated($"[grey]{combatLog.FileInfo}[/]")`. Either preserves "the filename shows when a new log is added" (do-not-break #3/#4). Pick one and keep it consistent.
- Leave `ListCombatLogs` UNCHANGED (iterates `CombatLogs.EnumerateCombatLogs()` and `Console.WriteLine`s — do-not-break #3).

Then build: `dotnet build SwtorLogParser.Cli/SwtorLogParser.Cli.csproj`, then the full solution `dotnet build SwtorLogParser.slnx`, then `dotnet test SwtorLogParser.Tests/SwtorLogParser.Tests.csproj`.
  </action>
  <verify>
    <automated>dotnet build SwtorLogParser.slnx succeeds; dotnet test SwtorLogParser.Tests/SwtorLogParser.Tests.csproj exits 0 with 106 passing, zero skips. SwtorLogParser.Cli/Program.cs contains "Spectre.Console", "AnsiConsole", "Console.CancelKeyPress", the five headers "Player"/"dps"/"(crit %)"/"hps", and NO "System.CommandLine" / "ConsoleRenderer" / "TableView" / "Region". The Cli.csproj contains "Spectre.Console", NO "System.CommandLine", NO "DockerDefaultTargetOS". A repo-wide search finds NO "DockerDefaultTargetOS" in any csproj.</automated>
  </verify>
  <done>Managed CLI builds and tests green; renders the 5-column table via Spectre.Console; Ctrl+C bridge wired; System.CommandLine.Rendering + Docker prop removed; Spectre referenced only by the managed CLI; columns/format/order preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Human-verify — AOT publish succeeds + Ctrl+C stops monitor cleanly on both hosts</name>
  <action>Manual verification only — these cannot be automated. Run the Native AOT publish and the interactive Ctrl+C / list smoke checks described in &lt;how-to-verify&gt; and confirm clean results before resuming.</action>
  <what-built>
Both CLI hosts now use hand-rolled dispatch with a Console.CancelKeyPress → CancellationTokenSource Ctrl+C bridge, the managed CLI renders via Spectre.Console, and the Native CLI keeps its PERF-02 renderer. The two checks below cannot be automated — Ctrl+C needs an interactive console and the AOT publish needs the MSVC/ILCompiler toolchain (05-VALIDATION "Manual-Only Verifications", 05-RESEARCH Pitfall 3 + Environment Availability).
  </what-built>
  <how-to-verify>
1. Native AOT publish (DEP-01/DEP-03 + AOT invariant):
   Run `dotnet publish SwtorLogParser.Native.Cli -c Release`.
   Expect: succeeds, produces a native executable, NO new trim/AOT (IL2xxx/IL3xxx) warnings about removed deps. If it fails with NETSDK1045 (missing net8.0 targeting pack) or an ILCompiler/MSVC error, that is an environment gap (Pitfall 5) — install the .NET 8 SDK / VS C++ workload and retry; the code change itself is correct if the managed build + tests passed.
2. Ctrl+C clean stop — managed CLI (DEP-03):
   Run `dotnet run --project SwtorLogParser.Cli -- monitor`. Confirm the Spectre table renders the 5 columns (Player, dps, (crit %), hps, (crit %)). Press Ctrl+C. Expect: the process exits promptly and cleanly (Stop() ran, no hang, no unhandled exception).
3. Ctrl+C clean stop — Native CLI (DEP-03):
   Run `dotnet run --project SwtorLogParser.Native.Cli -- monitor`. Press Ctrl+C. Expect: prompt clean exit, in-place row renderer behaved as before.
4. list parity (both hosts):
   `dotnet run --project SwtorLogParser.Cli -- list` and `dotnet run --project SwtorLogParser.Native.Cli -- list` each print the combat-log files (same as before).
  </how-to-verify>
  <resume-signal>Type "approved" once the AOT publish succeeds and both hosts stop cleanly on Ctrl+C; or describe what failed (e.g. "Ctrl+C hangs on Native" / "AOT publish NETSDK1045").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → host dispatch | Untrusted argv enters the switch; validated against a fixed allow-list |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | `args[0]` dispatch in both Program.cs | mitigate | Fixed allow-list switch (`list`/`monitor`); default branch writes usage to stderr and returns exit 1. No arg is interpolated into a shell, path, or query (05-RESEARCH Security Domain V5). |
| T-05-02-02 | Denial of Service | `monitor` Ctrl+C handling | mitigate | `Console.CancelKeyPress` sets `e.Cancel = true` and cancels the CTS so `Stop()` runs cleanly instead of the process being killed mid-write (Pitfall 3). |
| T-05-SC | Tampering | Spectre.Console 0.57.0 (only new dependency, managed host only) | mitigate | Verified GA, 44.6M downloads, official spectreconsole/spectre.console repo, named maintainers (Package Legitimacy Audit). Base `Table` only — `Spectre.Console.Cli` (reflection/AOT-unsafe) is NOT used. Confined to the non-AOT managed CLI. Not `[ASSUMED]`/`[SUS]`/`[SLOP]`, so no blocking package-legitimacy checkpoint required. |
</threat_model>

<verification>
- `dotnet build SwtorLogParser.slnx` succeeds with no System.CommandLine* anywhere.
- `dotnet test SwtorLogParser.Tests/SwtorLogParser.Tests.csproj` → 106 passing, zero skips (behavior preserved).
- Grep: no `System.CommandLine` / `System.CommandLine.Rendering` reference in either CLI csproj or Program.cs; no `DockerDefaultTargetOS` in any csproj; `Spectre.Console` present only in the managed CLI csproj.
- MANUAL (checkpoint): `dotnet publish SwtorLogParser.Native.Cli -c Release` AOT succeeds with no new trim warnings; Ctrl+C cleanly stops `monitor` on BOTH hosts; managed CLI shows the 5-column Spectre table; `list` parity on both hosts.
</verification>

<success_criteria>
- ROADMAP Phase 5 criterion 3 satisfied: managed CLI renders its live table without `System.CommandLine.Rendering`, using Spectre.Console; it compiles and produces visible output.
- ROADMAP Phase 5 criterion 4 satisfied: `DockerDefaultTargetOS=Linux` absent from all csproj.
- DEP-03 + INFRA-02 implemented; both hosts keep identical commands, columns, and Ctrl+C → Stop() behavior; Native CLI still AOT-publishes and stays Spectre-free.
- 106 tests green, zero skips; solution builds.
</success_criteria>

<output>
Create `.planning/phases/05-dependency-upgrades/05-02-SUMMARY.md` when done.
</output>
