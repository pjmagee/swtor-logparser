---
phase: 08-winui-3-scaffold-dependencies-guardrails
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - .github/workflows/ci.yml
autonomous: false
requirements: [OVL-01]

must_haves:
  truths:
    - "dotnet build SwtorLogParser.slnx -c Release succeeds with the WinUI 3 project added"
    - "dotnet publish SwtorLogParser.Native.Cli -c Release stays warning-free (no new IL2xxx/IL3xxx trim/AOT warnings)"
    - "The published WinUI overlay launches an empty window from its published folder on a clean profile (not just F5/VS)"
    - "CI on windows-latest restores and builds the .slnx including the WinUI project (workflow updated if a workload/restore gap exists)"
    - "The existing WinForms overlay still builds within the full .slnx build"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI that restores + builds the full .slnx (incl. the WinUI 3 project) on windows-latest, keeping the Native AOT publish gate"
      contains: "SwtorLogParser.slnx"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "SwtorLogParser.Overlay.WinUi.csproj"
      via: "dotnet restore + dotnet build SwtorLogParser.slnx pulls in the WinUI project"
      pattern: "dotnet build SwtorLogParser.slnx"
    - from: "Native AOT publish gate"
      to: "SwtorLogParser.Native.Cli"
      via: "dotnet publish SwtorLogParser.Native.Cli -c Release re-run as AOT-contamination regression check"
      pattern: "dotnet publish SwtorLogParser.Native.Cli"
---

<objective>
Prove the guardrails hold now that the WinUI 3 project exists: the full `.slnx` builds green (incl. WinUI), the Native AOT publish stays warning-free, CI on `windows-latest` restores/builds the new project, the WinForms overlay still builds, and the published overlay launches an empty window from a clean-profile published folder.

Purpose: Close the second half of OVL-01 — the AOT/CI guardrails and the "launches from a published folder on a clean profile (not just F5)" success criterion. This is the regression gate that resolves the known open question (does headless windows-latest restore/build an unpackaged self-contained WinUI 3 project without a separate workload install?) before the phase is considered done.

Output: A verified-green `dotnet build SwtorLogParser.slnx -c Release`; a warning-free `dotnet publish SwtorLogParser.Native.Cli -c Release`; an updated `.github/workflows/ci.yml` (only if a restore/workload gap is found — otherwise unchanged); a human-confirmed empty-window launch from the published folder.
</objective>

<execution_context>
@D:/Projects/pjmagee/swtor-logparser/.claude/gsd-core/workflows/execute-plan.md
@D:/Projects/pjmagee/swtor-logparser/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/research/PITFALLS.md
@.planning/research/STACK.md
@.github/workflows/ci.yml
@SwtorLogParser.Native.Cli/SwtorLogParser.Native.Cli.csproj
@SwtorLogParser.slnx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify full .slnx build + AOT regression gate; wire CI for the WinUI project if needed</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    Run the two regression gates locally and confirm both are green now that the WinUI project (Plan 01) is in the solution:

    1. `dotnet restore SwtorLogParser.slnx` then `dotnet build SwtorLogParser.slnx -c Release --no-restore` — must succeed (success criterion 3). This builds all six projects including the new WinUI project AND the unchanged WinForms `SwtorLogParser.Overlay` (success criterion 5 — WinForms parity safety net). If the build fails specifically on the WinUI project for a missing Windows App SDK workload/MSIX build task on the headless runner (the known open question / Pitfall 6 CI trap), resolve it minimally: prefer the self-contained config already set in Plan 01 (which should restore WinAppSDK from NuGet without a separate workload); only if restore genuinely needs it, add a `dotnet workload restore` (or equivalent `Microsoft.WindowsAppSDK` workload install) step to `ci.yml` BEFORE the build step. Make the smallest change that keeps the runner green — do NOT exclude the WinUI project from the build.

    2. `dotnet publish SwtorLogParser.Native.Cli -c Release` — must complete with NO new trim/AOT warnings (IL2xxx/IL3xxx) (success criterion 4 / Pitfall 7 AOT-contamination guard). The new packages must not have leaked into the Native CLI dependency graph. If new warnings appear, treat as a contamination regression: confirm WinAppSDK/CsWin32 are referenced only from the WinUI project (no transitive path into core/Native CLI) and fix before proceeding.

    Update `.github/workflows/ci.yml` only if step 1 required a workload/restore change. The existing `build-test` job already runs `dotnet build SwtorLogParser.slnx -c Release` and the `aot-publish` job already runs `dotnet publish SwtorLogParser.Native.Cli -c Release` on `windows-latest` — if both pass unchanged with the WinUI project present, leave the workflow as-is and record that no change was needed. Do NOT alter the test, coverage, or AOT jobs beyond any workload step.
  </action>
  <verify>
    <automated>dotnet build SwtorLogParser.slnx -c Release 2>&1 | grep -Eq 'Build succeeded|0 Error' && dotnet publish SwtorLogParser.Native.Cli -c Release 2>&1 | tee /tmp/aot_publish.log | grep -Eqv 'IL2[0-9]{3}|IL3[0-9]{3}' && ! grep -Eq 'IL2[0-9]{3}|IL3[0-9]{3}' /tmp/aot_publish.log && echo OK</automated>
  </verify>
  <done>`dotnet build SwtorLogParser.slnx -c Release` succeeds (all six projects, incl. WinUI and WinForms); `dotnet publish SwtorLogParser.Native.Cli -c Release` emits no IL2xxx/IL3xxx warnings; ci.yml either unchanged (recorded as "no change needed") or contains the minimal workload step that keeps windows-latest green.</done>
</task>

<task type="auto">
  <name>Task 2: Publish the WinUI overlay self-contained and confirm the launch artifact exists</name>
  <files>SwtorLogParser.Overlay.WinUi/SwtorLogParser.Overlay.WinUi.csproj</files>
  <action>
    Produce the published, self-contained output that the clean-profile launch check (Task 3) will run, to prove the unpackaged self-contained config actually emits a runnable `.exe` with the Windows App SDK runtime alongside it (Pitfall 6 — the "won't start outside VS" trap). Run `dotnet publish SwtorLogParser.Overlay.WinUi -c Release -r win-x64 --self-contained` (the project already sets `WindowsAppSDKSelfContained=true` and `RuntimeIdentifiers` from Plan 01). Confirm the publish folder contains `SwtorLogParser.Overlay.WinUi.exe` plus the Windows App SDK runtime assemblies (so it does not depend on a machine-installed runtime). Do NOT modify the project configuration unless the publish reveals a missing self-contained knob; if it does, fix the csproj minimally and note it. This task does not change behavior — the window is still empty.
  </action>
  <verify>
    <automated>dotnet publish SwtorLogParser.Overlay.WinUi -c Release -r win-x64 --self-contained 2>&1 | grep -Eq 'Build succeeded|published|0 Error' && find SwtorLogParser.Overlay.WinUi -path '*publish*' -name 'SwtorLogParser.Overlay.WinUi.exe' | grep -q . && echo OK</automated>
  </verify>
  <done>`dotnet publish` of the WinUI overlay produces a self-contained `SwtorLogParser.Overlay.WinUi.exe` in a publish folder with the WinAppSDK runtime present alongside it.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the published overlay launches an empty window on a clean profile</name>
  <files>(none — manual launch verification of the Task 2 publish output)</files>
  <action>
    PAUSE for the user to verify the published WinUI overlay launches outside Visual Studio. The executor has already produced the self-contained publish folder in Task 2; this task only collects the human confirmation that resolves the "launches from a published folder on a clean profile (not just F5)" half of success criterion 2 and the Pitfall 6 "won't start outside VS" trap. Present the verification steps below to the user and wait for the resume signal. The window is intentionally EMPTY this phase (no stats/transparency/interop — those are Phases 9-10).
  </action>
  <what-built>An unpackaged, self-contained WinUI 3 overlay project (`SwtorLogParser.Overlay.WinUi`) published to a self-contained folder by Task 2. The window is intentionally EMPTY this phase.</what-built>
  <how-to-verify>
    1. Open the publish output folder for the WinUI overlay (the `...\publish\` directory produced by Task 2, e.g. `SwtorLogParser.Overlay.WinUi\bin\Release\net10.0-windows10.0.19041.0\win-x64\publish\`).
    2. Double-click `SwtorLogParser.Overlay.WinUi.exe` directly from that folder (NOT via F5 / Visual Studio). Ideally test on a clean profile or a machine without the Windows App SDK runtime installed, to prove the self-contained packaging works.
    3. Confirm an empty window opens and stays open (no crash, no "Windows App Runtime not found" / bootstrapper error, no immediate exit).
    4. Close the window.
  </how-to-verify>
  <verify>
    <human-check>User confirms the empty window opened from the published `.exe` (double-clicked, not F5) and stayed open with no runtime-not-found / bootstrapper error.</human-check>
  </verify>
  <resume-signal>Type "approved" if the empty window opened from the published folder, or describe the error (e.g. "won't start" / runtime-not-found) so the self-contained config can be corrected.</resume-signal>
  <done>User has confirmed (resume signal "approved") that the published overlay `.exe` launches an empty window from its folder on a clean profile, not just under F5.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan produces NO new source files. It verifies guardrails and may edit one existing file.

| Artifact | Kind | Notes |
|----------|------|-------|
| `.github/workflows/ci.yml` | edited (conditionally) | Only if headless `windows-latest` needs a Windows App SDK workload/restore step to build the WinUI project; otherwise unchanged. No new jobs; existing build-test, coverage, and aot-publish jobs preserved. |

No new projects, XAML, manifests, or package entries are created here (all of those are in Plan 01). The WinUI publish output (`SwtorLogParser.Overlay.WinUi.exe` + self-contained runtime) is a build artifact, not a committed source file.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → build/publish | Headless `windows-latest` restores the new third-party packages and builds/publishes; no new network/auth/secret/untrusted-input surface (empty local window). Inherits the supply-chain boundary established in Plan 01. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering / contamination | Native AOT CLI dependency graph | mitigate | AOT publish re-run as a regression gate (Task 1): any IL2xxx/IL3xxx warning fails the gate and triggers a contamination fix (WinAppSDK/CsWin32 confined to the WinUI project). |
| T-08-05 | Denial of Service (build availability) | CI on windows-latest | mitigate | Full `.slnx` build verified green with the WinUI project; if a workload gap exists, the minimal `ci.yml` fix keeps the required build gate passing rather than silently excluding the project. |
| T-08-SC | Tampering | nuget installs (supply chain) | accept | Inherited from Plan 01 — first-party Microsoft, exact-pinned, GA packages; no `[ASSUMED]`/`[SUS]` packages; no blocking legitimacy checkpoint required. |

No high-severity threats. Per threat_model_note this phase adds packages and an empty local window only; nothing to block on.
</threat_model>

<verification>
- `dotnet build SwtorLogParser.slnx -c Release` is green with all six projects (WinUI + WinForms + core + 2 CLIs + tests).
- `dotnet publish SwtorLogParser.Native.Cli -c Release` emits no new IL2xxx/IL3xxx trim/AOT warnings (AOT-contamination guard).
- The WinUI overlay publishes self-contained and the human checkpoint confirms the empty window launches from the published folder on a clean profile.
- `ci.yml` keeps windows-latest green for the full `.slnx` (workload step added only if required).
- The WinForms overlay still builds as part of the full `.slnx` build (parity safety net).
</verification>

<success_criteria>
- `dotnet build SwtorLogParser.slnx -c Release` stays green on windows-latest with the WinUI 3 project added (OVL-01, criterion 3).
- `dotnet publish SwtorLogParser.Native.Cli -c Release` stays warning-free (OVL-01, criterion 4).
- The overlay launches an empty window from a published folder on a clean profile, not just F5 (OVL-01, criterion 2 — launch half).
- The WinForms overlay still builds and runs (OVL-01, criterion 5).
</success_criteria>

<output>
Create `.planning/phases/08-winui-3-scaffold-dependencies-guardrails/08-02-SUMMARY.md` when done.
</output>
