---
phase: 08-winui-3-scaffold-dependencies-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Directory.Packages.props
  - SwtorLogParser.Overlay.WinUi/SwtorLogParser.Overlay.WinUi.csproj
  - SwtorLogParser.Overlay.WinUi/app.manifest
  - SwtorLogParser.Overlay.WinUi/App.xaml
  - SwtorLogParser.Overlay.WinUi/App.xaml.cs
  - SwtorLogParser.Overlay.WinUi/MainWindow.xaml
  - SwtorLogParser.Overlay.WinUi/MainWindow.xaml.cs
  - SwtorLogParser.Overlay.WinUi/NativeMethods.txt
  - SwtorLogParser.slnx
autonomous: true
requirements: [OVL-01]

must_haves:
  truths:
    - "Microsoft.WindowsAppSDK 2.2.0 and Microsoft.Windows.CsWin32 0.3.275 are pinned as PackageVersion entries in Directory.Packages.props"
    - "A new project SwtorLogParser.Overlay.WinUi exists and is registered in SwtorLogParser.slnx"
    - "The WinUI project references both new packages version-less (CPM), CsWin32 PrivateAssets=all; the core lib and Native CLI gain no new package reference"
    - "dotnet build SwtorLogParser.Overlay.WinUi -c Release succeeds and produces a WinExe"
    - "The published overlay launches an empty borderless window"
    - "The existing WinForms SwtorLogParser.Overlay project is unmodified and still builds"
  artifacts:
    - path: "Directory.Packages.props"
      provides: "CPM version pins for Microsoft.WindowsAppSDK + Microsoft.Windows.CsWin32"
      contains: "Microsoft.WindowsAppSDK"
    - path: "SwtorLogParser.Overlay.WinUi/SwtorLogParser.Overlay.WinUi.csproj"
      provides: "Unpackaged self-contained WinUI 3 project; UseWinUI, WindowsPackageType=None, WindowsAppSDKSelfContained, AllowUnsafeBlocks; net10.0-windows10.0.19041.0"
      contains: "UseWinUI"
    - path: "SwtorLogParser.Overlay.WinUi/App.xaml.cs"
      provides: "WinUI Application bootstrap that creates and activates MainWindow"
      contains: "OnLaunched"
    - path: "SwtorLogParser.Overlay.WinUi/MainWindow.xaml"
      provides: "Empty WinUI render surface"
      contains: "Window"
    - path: "SwtorLogParser.Overlay.WinUi/NativeMethods.txt"
      provides: "CsWin32 generator input proving the package resolves and isolates (no runtime artifacts this phase)"
    - path: "SwtorLogParser.slnx"
      provides: "Solution registration of the new WinUI project"
      contains: "SwtorLogParser.Overlay.WinUi"
  key_links:
    - from: "SwtorLogParser.Overlay.WinUi.csproj"
      to: "Directory.Packages.props"
      via: "version-less PackageReference resolved via Central Package Management"
      pattern: "PackageReference Include=\"Microsoft.WindowsAppSDK\""
    - from: "App.xaml.cs"
      to: "MainWindow"
      via: "new MainWindow().Activate() in OnLaunched"
      pattern: "new MainWindow"
---

<objective>
Stand up a NEW, separate WinUI 3 (Windows App SDK) overlay project — `SwtorLogParser.Overlay.WinUi` — as an unpackaged, self-contained app that launches an empty window, and pin its two new dependencies (Windows App SDK + CsWin32) in Central Package Management referenced ONLY from this project.

Purpose: Establish the WinUI 3 host shell and the package-isolation boundary up front so later phases (9 live render, 10 CsWin32 interop) have a real window to build on — without disturbing the still-shipping WinForms overlay or contaminating the AOT-clean core/Native CLI (per D: keep WinForms building until Phase 11 parity gate). Satisfies the first half of OVL-01.

Output: New `Directory.Packages.props` entries (WinAppSDK 2.2.0, CsWin32 0.3.275); new `SwtorLogParser.Overlay.WinUi` project (csproj, app.manifest, App.xaml/.cs, MainWindow.xaml/.cs, NativeMethods.txt); slnx registration. Empty window builds and opens.
</objective>

<execution_context>
@D:/Projects/pjmagee/swtor-logparser/.claude/gsd-core/workflows/execute-plan.md
@D:/Projects/pjmagee/swtor-logparser/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@Directory.Packages.props
@SwtorLogParser.slnx
@SwtorLogParser.Overlay/SwtorLogParser.Overlay.csproj
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin WinAppSDK + CsWin32 in CPM and register the new project in the solution</name>
  <files>Directory.Packages.props, SwtorLogParser.slnx</files>
  <action>
    Add two `PackageVersion` entries to the existing `<ItemGroup>` in `Directory.Packages.props`: `Microsoft.WindowsAppSDK` Version `2.2.0` and `Microsoft.Windows.CsWin32` Version `0.3.275` (exact verified versions per STACK.md / success criterion 1 — do NOT bump or float). Leave all existing pins (System.Reactive 6.0.2, xunit, etc.) untouched. Do NOT add these as `PackageReference` to any project here — only the version pins. CPM unifies versions solution-wide, but the actual references will live ONLY in the WinUI project (Task 2); the core lib and Native CLI must gain no new reference (AOT-contamination guard — Pitfall 7).

    In `SwtorLogParser.slnx`, add one `<Project Path="SwtorLogParser.Overlay.WinUi/SwtorLogParser.Overlay.WinUi.csproj" />` line alongside the existing project entries (keep the existing 5 projects, including the WinForms `SwtorLogParser.Overlay`, unchanged — both overlays COEXIST per the phase coexistence constraint; identity consolidation is deferred to Phase 11).
  </action>
  <verify>
    <automated>grep -q 'Microsoft.WindowsAppSDK" Version="2.2.0"' Directory.Packages.props && grep -q 'Microsoft.Windows.CsWin32" Version="0.3.275"' Directory.Packages.props && grep -q 'SwtorLogParser.Overlay.WinUi/SwtorLogParser.Overlay.WinUi.csproj' SwtorLogParser.slnx && echo OK</automated>
  </verify>
  <done>Both new PackageVersion entries exist with the exact pinned versions; the new WinUI project is registered in the .slnx; no existing pins or project entries were removed; the WinForms Overlay entry is still present.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold the unpackaged self-contained WinUI 3 project with an empty window</name>
  <files>SwtorLogParser.Overlay.WinUi/SwtorLogParser.Overlay.WinUi.csproj, SwtorLogParser.Overlay.WinUi/app.manifest, SwtorLogParser.Overlay.WinUi/App.xaml, SwtorLogParser.Overlay.WinUi/App.xaml.cs, SwtorLogParser.Overlay.WinUi/MainWindow.xaml, SwtorLogParser.Overlay.WinUi/MainWindow.xaml.cs, SwtorLogParser.Overlay.WinUi/NativeMethods.txt</files>
  <action>
    Create the new WinUI 3 project under `SwtorLogParser.Overlay.WinUi/` (a SEPARATE directory from the WinForms `SwtorLogParser.Overlay/` — do NOT touch the WinForms project; that would break success criterion 5). RootNamespace `SwtorLogParser.Overlay.WinUi`.

    csproj (`Microsoft.NET.Sdk`, per STACK.md "Overlay project" shape and ARCHITECTURE.md csproj shape): `OutputType=WinExe`; `TargetFramework=net10.0-windows10.0.19041.0`; `TargetPlatformMinVersion=10.0.17763.0`; `RuntimeIdentifiers=win-x64;win-arm64`; `UseWinUI=true`; `EnableMsixTooling=true`; `WindowsPackageType=None` (unpackaged — auto-init bootstrapper, launches outside VS); `WindowsAppSDKSelfContained=true` (no separate runtime install — avoids the "won't start"/CI trap, Pitfall 6); `Nullable=enable`; `ImplicitUsings=enable`; `AllowUnsafeBlocks=true` (required by CsWin32-generated code, Pitfall 8). Do NOT add `PublishAot` or any trim flags (Pitfall 7 / AOT guard — the overlay is a managed JIT host). Reference the two packages version-less via CPM: `<PackageReference Include="Microsoft.WindowsAppSDK" />` and `<PackageReference Include="Microsoft.Windows.CsWin32">` with `<PrivateAssets>all</PrivateAssets>` and `<IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>` (CsWin32 is dev-only, produces no runtime artifacts — per critical constraints). Do NOT add a ProjectReference to the core library — an empty window does not need it, and the scope is scaffold only (Phase 9 wires the stream).

    `app.manifest`: standard WinUI per-monitor-v2 DPI-awareness + longPathAware manifest (the standard WinUI 3 template app.manifest). Reference it from the csproj via `<ApplicationManifest>app.manifest</ApplicationManifest>` if the template requires explicit wiring.

    `App.xaml` + `App.xaml.cs`: minimal `Microsoft.UI.Xaml.Application` subclass (`partial class App`), `InitializeComponent()` in the ctor, and `OnLaunched(LaunchActivatedEventArgs)` that does `new MainWindow().Activate()` and keeps a field reference to the window. Do NOT start `CombatLogsMonitor` or subscribe to any stream — scope is an empty window only (no DPS/HPS, no core dependency this phase).

    `MainWindow.xaml` + `MainWindow.xaml.cs`: minimal `Microsoft.UI.Xaml.Window` (`partial class MainWindow`) with `InitializeComponent()` and an empty root (a single empty `Grid`). No rendering, no transparency, no interop, no presenter customization yet (those are Phases 9-10). The window must simply open.

    `NativeMethods.txt`: create the CsWin32 input file. To keep this phase scaffold-only while still PROVING the CsWin32 package resolves and the source generator runs in isolation, add a single benign, broadly-available symbol (e.g. `GetForegroundWindow`) and a header comment noting the real interop surface (SetWindowPos, SetWinEventHook, WS_EX_*, etc.) lands in Phase 10. This generates a tiny `PInvoke` partial at compile time, confirming the generator works without pulling in runtime artifacts. Use `grep -v '^#'` semantics in the verify gate so the header comment does not self-invalidate the count.
  </action>
  <verify>
    <automated>dotnet build SwtorLogParser.Overlay.WinUi/SwtorLogParser.Overlay.WinUi.csproj -c Release 2>&1 | tee /tmp/winui_build.log | grep -Eq 'Build succeeded|0 Error' && grep -q 'UseWinUI' SwtorLogParser.Overlay.WinUi/SwtorLogParser.Overlay.WinUi.csproj && grep -q 'WindowsAppSDKSelfContained' SwtorLogParser.Overlay.WinUi/SwtorLogParser.Overlay.WinUi.csproj && grep -v '^#' SwtorLogParser.Overlay.WinUi/NativeMethods.txt | grep -cq '[A-Za-z]' && echo OK</automated>
  </verify>
  <done>`dotnet build SwtorLogParser.Overlay.WinUi -c Release` succeeds with zero errors; the csproj has UseWinUI, WindowsPackageType=None, WindowsAppSDKSelfContained, AllowUnsafeBlocks and the correct TFM; App.xaml.cs activates MainWindow; NativeMethods.txt contains at least one real CsWin32 symbol; no PublishAot/trim flags present; the WinForms Overlay project files are untouched.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New files/projects/properties created by THIS plan (Plan 01). Plan 02 adds no new source files — it only verifies guardrails and may edit `.github/workflows/ci.yml`.

| Artifact | Kind | Notes |
|----------|------|-------|
| `Directory.Packages.props` (2 new `PackageVersion`) | CPM entries | `Microsoft.WindowsAppSDK` 2.2.0; `Microsoft.Windows.CsWin32` 0.3.275 |
| `SwtorLogParser.Overlay.WinUi/SwtorLogParser.Overlay.WinUi.csproj` | new project | Unpackaged self-contained WinUI 3; UseWinUI / WindowsPackageType=None / WindowsAppSDKSelfContained / AllowUnsafeBlocks; net10.0-windows10.0.19041.0 |
| `SwtorLogParser.Overlay.WinUi/app.manifest` | new file | DPI-awareness + longPathAware |
| `SwtorLogParser.Overlay.WinUi/App.xaml` | new file | Application resources |
| `SwtorLogParser.Overlay.WinUi/App.xaml.cs` | new file | Application bootstrap; OnLaunched → MainWindow |
| `SwtorLogParser.Overlay.WinUi/MainWindow.xaml` | new file | Empty render surface (single Grid) |
| `SwtorLogParser.Overlay.WinUi/MainWindow.xaml.cs` | new file | Window code-behind |
| `SwtorLogParser.Overlay.WinUi/NativeMethods.txt` | new file | CsWin32 generator input (one benign symbol to prove resolution/isolation) |
| `SwtorLogParser.slnx` (1 new `<Project>`) | solution entry | Registers the WinUI project; WinForms Overlay entry retained |

New csproj properties of note: `UseWinUI`, `WindowsPackageType=None`, `WindowsAppSDKSelfContained=true`, `AllowUnsafeBlocks=true`, `EnableMsixTooling=true`, `RuntimeIdentifiers`, `TargetPlatformMinVersion`. No `launchSettings.json` is required this phase (unpackaged self-contained launches from the build output). Explicitly NOT produced this phase: ProjectReference to core, any DPS/HPS subscription, transparency/interop code, `Package.appxmanifest` (unpackaged).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NuGet feed → build | New third-party packages (WinAppSDK, CsWin32) enter the dependency graph via CPM. This is the only new boundary; the app has no network, auth, secrets, or untrusted runtime input this phase (empty local window). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | `Microsoft.WindowsAppSDK` 2.2.0 NuGet package | mitigate | Version pinned exactly (2.2.0) in CPM — no floating range; first-party Microsoft package; resolves from the default nuget.org feed. Verified provenance in STACK.md. |
| T-08-02 | Tampering | `Microsoft.Windows.CsWin32` 0.3.275 NuGet package | mitigate | Version pinned exactly (0.3.275); first-party Microsoft source generator, `PrivateAssets=all` (compile-time only, no runtime artifact flows transitively). Verified in STACK.md. |
| T-08-03 | Elevation of Privilege / contamination | AOT-clean core lib + Native CLI | mitigate | New packages referenced ONLY from the WinUI project; core/Native CLI gain no new PackageReference; AOT publish regression gate runs in Plan 02. |
| T-08-SC | Tampering | nuget installs (supply chain) | accept | Both packages are first-party Microsoft, exact-pinned, GA per STACK.md verification; no `[ASSUMED]`/`[SUS]` packages, so no blocking-human legitimacy checkpoint is required. Low-value local dev tool, no secrets/PII. |

No high-severity threats — this phase adds two first-party pinned packages and an empty local window; no network/auth/secrets/untrusted-input surface (per threat_model_note). Nothing to block on.
</threat_model>

<verification>
- `dotnet build SwtorLogParser.Overlay.WinUi -c Release` succeeds (empty window project compiles, CsWin32 generator runs).
- `Directory.Packages.props` contains exactly the two new pins at the verified versions; no other pin changed.
- The WinForms `SwtorLogParser.Overlay` project files are byte-for-byte unchanged (coexistence safety net intact).
- The new WinUI project has no `PublishAot`/trim flag and no ProjectReference to the core library.
- (Solution-wide build + AOT regression + published-folder launch + CI wiring are verified in Plan 02.)
</verification>

<success_criteria>
- Microsoft.WindowsAppSDK 2.2.0 and Microsoft.Windows.CsWin32 0.3.275 pinned in CPM, referenced only from the WinUI project (OVL-01, criterion 1).
- New `SwtorLogParser.Overlay.WinUi` project builds with UseWinUI / WindowsPackageType=None / WindowsAppSDKSelfContained / AllowUnsafeBlocks and the verified TFM (OVL-01, criterion 2 — build half; launch verified in Plan 02).
- WinForms overlay untouched and still building (OVL-01, criterion 5).
</success_criteria>

<output>
Create `.planning/phases/08-winui-3-scaffold-dependencies-guardrails/08-01-SUMMARY.md` when done.
</output>
