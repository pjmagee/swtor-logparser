---
phase: 03-monitor-refactor-coverage
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - SwtorLogParser/Monitor/CombatLogsMonitor.cs
  - SwtorLogParser.Tests/DpsHpsMathTests.cs
autonomous: true
requirements: [TEST-02]
must_haves:
  truths:
    - "Accumulator and CalculateDpsHpsStats are unit-testable directly (bypassing the DateTime.Now Where filter) for deterministic math"
    - "DPS, HPS, and crit% are asserted against known CombatLogLine inputs"
    - "The 10s sliding-window expiry in Accumulator is asserted (old lines removed, recent lines kept)"
  artifacts:
    - path: "SwtorLogParser/Monitor/CombatLogsMonitor.cs"
      provides: "Accumulator + CalculateDpsHpsStats made internal (visible via InternalsVisibleTo) — no behavior change"
      contains: "internal HashSet<CombatLogLine> Accumulator"
    - path: "SwtorLogParser.Tests/DpsHpsMathTests.cs"
      provides: "Deterministic DPS/HPS/crit% + 10s window expiry tests"
      contains: "DpsHpsMathTests"
  key_links:
    - from: "SwtorLogParser.Tests/DpsHpsMathTests.cs"
      to: "CombatLogsMonitor.CalculateDpsHpsStats"
      via: "direct internal method call (bypasses DateTime.Now Where)"
      pattern: "CalculateDpsHpsStats"
---

<objective>
Implement TEST-02: unit-test the DPS/HPS math (`Accumulator` + `CalculateDpsHpsStats`) DIRECTLY against known `CombatLogLine` inputs — asserting DPS, HPS, crit%, and the 10-second sliding-window expiry — by making those two methods `internal` (visible through the existing `InternalsVisibleTo(SwtorLogParser.Tests)`) WITHOUT changing their behavior.

Purpose: The full `DpsHps` Rx pipeline filters on `DateTime.Now` (line 51 `Where`), which silently drops fixed-literal timestamps and makes exact-number assertions flaky. Testing `Accumulator`/`CalculateDpsHpsStats` directly bypasses that filter and yields deterministic math assertions. This locks the CURRENT product behavior (it must NOT be improved — the accumulator full re-scan/re-sort is PERF-03/Phase 4).
Output: Two methods promoted to `internal` (visibility-only change) and `DpsHpsMathTests` asserting the math + window expiry.

Sequencing note: depends_on 03-01 because this plan also edits `CombatLogsMonitor.cs` (which Plan 01 modifies) — it runs after Plan 01 to avoid a same-file conflict in the same wave.
</objective>

<execution_context>
@D:/Projects/pjmagee/swtor-logparser/.claude/gsd-core/workflows/execute-plan.md
@D:/Projects/pjmagee/swtor-logparser/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-monitor-refactor-coverage/03-CONTEXT.md
@.planning/phases/03-monitor-refactor-coverage/03-RESEARCH.md
@.planning/phases/03-monitor-refactor-coverage/03-PATTERNS.md

# Target methods (current state — make internal, do NOT alter behavior)
@SwtorLogParser/Monitor/CombatLogsMonitor.cs
# Player-damage/heal predicates the test lines must satisfy
@SwtorLogParser/Extensions/CombatLogLineExtensions.cs
# Test analog shape
@SwtorLogParser.Tests/CombatLogsMonitorTests.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make Accumulator + CalculateDpsHpsStats internal (behavior-preserving) and add deterministic DPS/HPS math tests</name>
  <files>SwtorLogParser/Monitor/CombatLogsMonitor.cs, SwtorLogParser.Tests/DpsHpsMathTests.cs</files>
  <read_first>
    - SwtorLogParser/Monitor/CombatLogsMonitor.cs (`Accumulator` lines 64-75: `lock (Lock)`, `state.RemoveWhere(line => line.TimeStamp < combatLog.TimeStamp.AddSeconds(-10))`, `state.Add`; `CalculateDpsHpsStats` lines 77-108: orders by TimeOfDay, splits heals/damage via `IsPlayerHeal`/`IsPlayerDamage`, `timeSpan = items.Count > 1 ? last-first : 1s`, `damageTotal`/`healTotal`, crit% = `count(IsCritical)/state.Count*100`, dps/hps = total/timeSpan.TotalSeconds, null-on-zero/infinity, `PlayerStats.Player = state.ElementAt(0).Source!`)
    - SwtorLogParser/Extensions/CombatLogLineExtensions.cs (`IsPlayerDamage` = Source.IsPlayer && Action equals `Action.ApplyEffectDamage` && Value present; `IsPlayerHeal` = the Heal equivalent)
    - SwtorLogParser.Tests/CombatLogsMonitorTests.cs (golden damage/heal lines at lines 26-27 — copy their SHAPE for constructing known inputs); 03-RESEARCH.md "Direct math test (deterministic...)" code example + Pitfall 1 strategy 1; 03-PATTERNS.md "DPS/HPS math under test (TEST-02)"
  </read_first>
  <behavior>
    - Dps_Computed_From_Known_Damage: two known player-damage lines with controlled relative timestamps → assert `CalculateDpsHpsStats(state).DPS` equals `damageTotal / (last-first).TotalSeconds` (precision ~3).
    - Hps_Computed_From_Known_Heals: analogous for heals → assert `HPS`.
    - Crit_Percent_Computed: include a critical-valued line and assert `DPSCritP`/`HPSCritP` matches `count(IsCritical)/state.Count*100` (and that exactly-zero/infinity maps to null per the current code).
    - Window_Expiry_Removes_Old_Lines: build state, then `Accumulator(state, newLine)` where an existing line's TimeStamp is >10s older than `newLine.TimeStamp` → assert the old line is removed and the new one kept (locks the 10s `RemoveWhere`).
  </behavior>
  <action>
    Per D-TEST-02 (CONTEXT "Test coverage"):
    1. In `CombatLogsMonitor.cs` change `private HashSet<CombatLogLine> Accumulator(...)` (line 64) and `private PlayerStats CalculateDpsHpsStats(...)` (line 77) to `internal`. This is a VISIBILITY-ONLY change reachable via the existing `InternalsVisibleTo(SwtorLogParser.Tests)` (RESEARCH Assumption A2). DO NOT modify the method bodies, the `lock (Lock)`, the 10s `RemoveWhere`, the crit% formula, the order-by-TimeOfDay, or the null-on-zero/infinity logic — this is the live product behavior on the Do-Not-Break list and any change is a regression (and PERF-03 is Phase 4, explicitly out of scope).
    2. Add `SwtorLogParser.Tests/DpsHpsMathTests.cs` (namespace `SwtorLogParser.Tests`, `using SwtorLogParser.Model; using SwtorLogParser.Monitor;`). Construct the monitor via the public ctor from Plan 01 (`new CombatLogsMonitor(NullLogger<CombatLogsMonitor>.Instance)`) to reach the internal methods. Build a `HashSet<CombatLogLine>` of KNOWN lines parsed via `CombatLogLine.Parse(...)` shaped like the golden lines (player Source `@Name#id|...`, `ApplyEffect ...: Damage ...` / `: Heal ...` action, a `(NNN)` value, `<...>` for crit where applicable). Call `monitor.Accumulator(state, line)` to build state and `monitor.CalculateDpsHpsStats(state)` to assert.
    3. CRITICAL (Pitfall 1 strategy 1): because we call the math methods DIRECTLY, the `DateTime.Now` `Where` filter is bypassed — so fixed/controlled relative timestamps are deterministic here (unlike the pipeline test in Plan 01). Use timestamps with a KNOWN delta between them (e.g. two lines 1.0s apart) so `timeSpan` and therefore DPS is an exact expected value. Assert numbers with a small precision tolerance (`Assert.Equal(expected, actual.Value, precision: 3)`).
    4. For the window-expiry test, exploit the 10s rule in `Accumulator`: seed `state` with a line at `tOld`, then `Accumulator(state, newLine)` where `newLine.TimeStamp >= tOld.AddSeconds(11)`; assert `tOld`'s line was removed (`state` no longer contains it) and `newLine` is present.
    Keep the tests as plain `[Fact]`s; lock current behavior only — do NOT assert any "improved" rounding/averaging.
  </action>
  <verify>
    <automated>dotnet test SwtorLogParser.Tests/SwtorLogParser.Tests.csproj --filter "FullyQualifiedName~DpsHpsMathTests" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `CombatLogsMonitor.cs` declares `internal HashSet<CombatLogLine> Accumulator(` and `internal PlayerStats CalculateDpsHpsStats(` (no other body change — the 10s `RemoveWhere`, crit% formula, and null-on-zero logic are byte-identical to before).
    - `DpsHpsMathTests.cs` exists with DPS, HPS, crit%, and 10s-window-expiry assertions against known inputs, computed by calling the internal methods directly.
    - `dotnet test` exits 0; the math tests pass deterministically with no `DateTime.Now` flakiness.
  </acceptance_criteria>
  <done>DPS/HPS/crit% and the 10s window expiry are locked by deterministic direct-method tests; no production behavior changed beyond method visibility.</done>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces

**New file:**
- `SwtorLogParser.Tests/DpsHpsMathTests.cs` — deterministic DPS/HPS/crit% + 10s sliding-window expiry tests calling the internal math methods directly.

**Modified type:**
- `SwtorLogParser.Monitor.CombatLogsMonitor` — `Accumulator` and `CalculateDpsHpsStats` changed from `private` to `internal` (visibility-only; bodies unchanged).

**No new namespaces, no behavior change, no IClock/TimeProvider, no WinForms/DI/reflection.**
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local log line → math methods | Already-parsed CombatLogLine inputs; no untrusted boundary crossed in the test path |

No network, auth, session, secret, or crypto surface in this phase.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | malformed line into DPS math | accept | Inputs are parsed via the null-return-hardened factories (Phase 2); math methods only see valid lines |
| T-03-SC | Tampering | npm/pip/cargo installs | n/a | No package installs; visibility change + new test only |
</threat_model>

<verification>
- `dotnet test SwtorLogParser.Tests/SwtorLogParser.Tests.csproj` exits 0, 0 skipped (math tests green; suite not regressed).
- `Accumulator`/`CalculateDpsHpsStats` are `internal` and otherwise unchanged (diff shows only the visibility keyword).
- No `DateTime.Now`-driven flakiness: math tests call the methods directly and assert exact numbers within precision.
</verification>

<success_criteria>
- TEST-02 success criterion (ROADMAP #5): automated tests verify DPS/HPS arithmetic against known inputs and sliding-window expiry behavior.
- Current behavior is locked, not improved (PERF-03 remains Phase 4).
- Suite green, zero skips.
</success_criteria>

<output>
Create `.planning/phases/03-monitor-refactor-coverage/03-04-SUMMARY.md` when done.
</output>
