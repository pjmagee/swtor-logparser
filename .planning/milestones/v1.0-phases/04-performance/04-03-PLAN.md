---
phase: 04-performance
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - SwtorLogParser/Monitor/CombatLogsMonitor.cs
  - SwtorLogParser.Tests/DpsHpsMathTests.cs
autonomous: true
requirements: [PERF-03]

must_haves:
  truths:
    - "CalculateDpsHpsStats computes DPS, HPS, crit%, and null-on-zero/infinity IDENTICALLY to the locked original (DpsHpsMathTests pass unchanged)."
    - "The OrderBy(TimeOfDay) sort and the 6 separate Where/Sum/Count LINQ passes are replaced by a SINGLE foreach over `state`."
    - "min/max timestamps are tracked by `.TimeStamp.TimeOfDay` (the same key the old OrderBy used) — the across-midnight quirk is PRESERVED, not fixed (that is IN-01, out of scope)."
    - "crit% divides by `state.Count` (NOT damage/heal count); damage and heal use INDEPENDENT ifs (not else-if); timeSpan == 1s when count<=1; Player from any element."
    - "The Accumulator (lock, 10s RemoveWhere, Add) and the DateTime.Now window filter are UNCHANGED."
    - "dotnet test stays green with zero skips after every commit (DpsHpsMathTests + the new single-line edge test)."
  artifacts:
    - path: "SwtorLogParser/Monitor/CombatLogsMonitor.cs"
      provides: "Single-pass CalculateDpsHpsStats (no OrderBy, no multi-pass LINQ); identical output"
      contains: "foreach"
    - path: "SwtorLogParser.Tests/DpsHpsMathTests.cs"
      provides: "New Single_Line_Uses_OneSecond_Window edge test locking the count<=1 ⇒ 1s branch"
      contains: "Single_Line_Uses_OneSecond_Window"
  key_links:
    - from: "SwtorLogParser/Monitor/CombatLogsMonitor.cs CalculateDpsHpsStats"
      to: "PlayerStats (DPS/HPS/DPSCritP/HPSCritP/Player)"
      via: "single foreach min/max + per-category sum/count → same four output formulas"
      pattern: "foreach \\(var .* in state\\)"
---

<objective>
PERF-03: Collapse `CalculateDpsHpsStats` from `OrderBy(x => x.TimeStamp.TimeOfDay)` + 6 separate `Where`/`Sum`/`Count` LINQ passes into a SINGLE `foreach` over `state` that tracks min/max timestamp + per-category sum/count/crit, then applies the SAME four output formulas verbatim. The output MUST be byte-for-byte identical — the Phase 3 TEST-02 `DpsHpsMathTests` (7 facts after this plan) are the contract.

CRITICAL equivalence rules (from 04-RESEARCH / 04-PATTERNS DO-NOT-BREAK):
- crit% divides by `state.Count` (NOT damage/heal count).
- damage and heal use INDEPENDENT `if`s (NOT `else if`) — matches the original's independent `Where` passes (Pitfall 3).
- min/max tracked by `.TimeStamp.TimeOfDay` (the same key the old `OrderBy` used) — preserves the across-midnight quirk; do NOT "fix" it (that is IN-01, out of scope) (Pitfall 4).
- `timeSpan == TimeSpan.FromSeconds(1)` when `state.Count <= 1`.
- `Player` from any element's `Source` (order-invariant because `GroupBy(Source.Name)` upstream guarantees one player per state).
- Keep the `lock`. Do NOT touch the `Accumulator` body or the `DateTime.Now` `.Where` window filter.

The Wave-0 edge test (`Single_Line_Uses_OneSecond_Window`) is added FIRST to lock the `count<=1 ⇒ 1s` branch the rewrite must preserve.

Purpose: Replace O(n log n) sort + 6 scans per line with one O(n) scan over a ≤10s window — identical numbers (PERF-03).
Output: Refactored `CalculateDpsHpsStats` (single pass) + a new edge test in `DpsHpsMathTests.cs`.
</objective>

<execution_context>
@D:/Projects/pjmagee/swtor-logparser/.claude/gsd-core/workflows/execute-plan.md
@D:/Projects/pjmagee/swtor-logparser/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-performance/04-CONTEXT.md
@.planning/phases/04-performance/04-RESEARCH.md
@.planning/phases/04-performance/04-PATTERNS.md
@.planning/phases/04-performance/04-VALIDATION.md
@.planning/phases/03-monitor-refactor-coverage/03-04-SUMMARY.md

# Target + the locked test contract (read first)
@SwtorLogParser/Monitor/CombatLogsMonitor.cs
@SwtorLogParser.Tests/DpsHpsMathTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 edge test — Single_Line_Uses_OneSecond_Window (write FIRST, GREEN against current code)</name>
  <files>SwtorLogParser.Tests/DpsHpsMathTests.cs</files>
  <read_first>
    - SwtorLogParser.Tests/DpsHpsMathTests.cs (the 7-fact contract; `DamageLine`/`HealLine` builders; `NewMonitor()`; the `Accumulator` + `CalculateDpsHpsStats` direct-call pattern that bypasses the DateTime.Now filter)
    - SwtorLogParser/Monitor/CombatLogsMonitor.cs (CalculateDpsHpsStats lines 87-118 — the `items.Count > 1 ? ... : TimeSpan.FromSeconds(1)` branch at line 95-96)
    - 04-RESEARCH.md Phase Requirements → Test Map (the `Single_Line_Uses_OneSecond_Window` Wave-0 gap) + 03-04-SUMMARY.md (how the existing math tests are constructed)
  </read_first>
  <action>
    Add ONE `[Fact] Single_Line_Uses_OneSecond_Window` to SwtorLogParser.Tests/DpsHpsMathTests.cs (do not modify the existing 6 facts). It locks the `count <= 1 ⇒ timeSpan == 1s` branch explicitly (currently covered only transitively), which the single-pass rewrite in Task 2 must preserve.

    Build a single damage line (e.g. `DamageLine("20:00:00.000", 1500, critical: false)`), `Accumulator` it into a fresh `HashSet<CombatLogLine>(new CombatLogLineComparer())`, call `monitor.CalculateDpsHpsStats(state)`, and assert: `Assert.Single(state)`, `Assert.NotNull(stats.DPS)`, and `Assert.Equal(1500d, stats.DPS!.Value, precision: 3)` — i.e. with one line the divisor is 1.0s (`timeSpan == TimeSpan.FromSeconds(1)`), so DPS == the single line's value. Also assert `Assert.Null(stats.HPS)` (no heals). Mirror the existing facts' style (NewMonitor(), the builders, `precision: 3`).

    Run `dotnet test ... --filter "FullyQualifiedName~DpsHpsMathTests"` — all 7 facts (6 existing + 1 new) MUST be GREEN against the CURRENT unmodified `CalculateDpsHpsStats`. Commit: `test(04-03): PERF-03 lock count<=1 ⇒ 1s window edge (Single_Line_Uses_OneSecond_Window)`.
  </action>
  <verify>
    <automated>dotnet test SwtorLogParser.Tests/SwtorLogParser.Tests.csproj --nologo --filter "FullyQualifiedName~DpsHpsMathTests"</automated>
  </verify>
  <done>`DpsHpsMathTests.cs` has `Single_Line_Uses_OneSecond_Window`; all 7 DpsHpsMathTests facts pass against the current unmodified CalculateDpsHpsStats; full suite green with zero skips.</done>
</task>

<task type="auto">
  <name>Task 2: Single-pass CalculateDpsHpsStats (PERF-03) — identical output</name>
  <files>SwtorLogParser/Monitor/CombatLogsMonitor.cs</files>
  <read_first>
    - SwtorLogParser/Monitor/CombatLogsMonitor.cs (the LOCKED original CalculateDpsHpsStats at lines 87-118; the Accumulator at lines 71-82 that must NOT change; the `DateTime.Now` window filter at line 54 that must NOT be touched)
    - SwtorLogParser.Tests/DpsHpsMathTests.cs (the 7-fact contract these numbers must satisfy unchanged — DPS 3000, HPS 2000, crit 50%, zero-crit→null, window expiry/keep, single-line 1s)
    - 04-RESEARCH.md Pattern 2 (single-pass aggregation, with the exact equivalence mapping line-by-line to the original) + Pitfalls 3 (independent ifs), 4 (TimeOfDay min/max, midnight quirk preserved); the verified-original excerpt at "Code Examples"
    - 04-PATTERNS.md CombatLogsMonitor section (EXACT current code + the "Output-identity rules" + the ExtractPosition single-scan analog) + DO-NOT-BREAK items 1-5
  </read_first>
  <action>
    Rewrite the BODY of `CalculateDpsHpsStats(HashSet<CombatLogLine> state)` in SwtorLogParser/Monitor/CombatLogsMonitor.cs as a SINGLE `foreach (var line in state)` (keep the method `internal`, keep the signature, keep the `PlayerStats` return shape). Per the locked equivalence rules:

    1. Track, in locals initialized before the loop: min/max timestamp by `.TimeStamp.TimeOfDay` (e.g. `TimeSpan minTod = TimeSpan.MaxValue, maxTod = TimeSpan.MinValue;` updated with `if (tod < minTod) minTod = tod; if (tod > maxTod) maxTod = tod;`) — this matches the dropped `OrderBy(x => x.TimeStamp.TimeOfDay)` endpoints AND preserves the across-midnight quirk (Pitfall 4; do NOT switch to full DateTime, do NOT correct the quirk — that is IN-01). Also keep the full `DateTime` for whichever line is min/max if you compute `timeSpan` from `TimeStamp` deltas — but the ORDERING/selection key must be `TimeOfDay`. Simplest correct approach: track the min-by-TimeOfDay line's `TimeStamp` and the max-by-TimeOfDay line's `TimeStamp`, then `timeSpan = maxLine.TimeStamp - minLine.TimeStamp` (equivalent to the old `items[^1].TimeStamp - items[0].TimeStamp`).
    2. `damageCount`, `damageTotal`, `damageCrit`, `healCount`, `healTotal`, `healCrit` as int locals. In the loop use TWO INDEPENDENT ifs (NOT else-if): `if (line.IsPlayerDamage()) { damageCount++; damageTotal += line.Value!.Total; if (line.Value!.IsCritical) damageCrit++; }` and a SEPARATE `if (line.IsPlayerHeal()) { ... }` (Pitfall 3 — matches the original's independent `Where` passes).
    3. `Actor? player = null; player ??= line.Source;` inside the loop (any element — order-invariant via upstream GroupBy(Source.Name); replaces `state.ElementAt(0).Source`).
    4. After the loop, compute IDENTICALLY to the original:
       - `var timeSpan = state.Count > 1 ? (maxLine.TimeStamp - minLine.TimeStamp) : TimeSpan.FromSeconds(1);` (guard on `state.Count`, same as old `items.Count`).
       - `double dpsCrit = (double)damageCrit / state.Count * 100;` and `double hpsCrit = (double)healCrit / state.Count * 100;` (divisor is `state.Count`, NOT damageCount/healCount — DO-NOT-BREAK item 2).
       - `double? dps = damageCount > 0 ? damageTotal / timeSpan.TotalSeconds : null;` and the heal analog.
       - `double? dpsCritP = double.IsInfinity(dpsCrit) || dpsCrit == 0.0d ? null : dpsCrit;` and the heal analog (null-maps zero AND infinity — verbatim).
       - `return new PlayerStats { Player = player!, DPS = dps, HPS = hps, DPSCritP = dpsCritP, HPSCritP = hpsCritP };`

    Do NOT change the `Accumulator` (keep `static readonly object Lock`, the 10s `RemoveWhere(line => line.TimeStamp < combatLog.TimeStamp.AddSeconds(-10))`, the `Add`). Do NOT touch the `DateTime.Now` `.Where(x => x.TimeStamp > DateTime.Now.AddSeconds(-10))` filter in `ConfigureObservables` (IN-01, deferred). No new `using`, no reflection — core lib stays AOT-safe.

    Run the full suite; every DpsHpsMathTests number MUST be unchanged. If ANY number moves, revert and re-derive against the original formulas. Commit: `perf(04-03): PERF-03 single-pass CalculateDpsHpsStats (no OrderBy/multi-pass LINQ), identical output`.
  </action>
  <verify>
    <automated>dotnet test SwtorLogParser.Tests/SwtorLogParser.Tests.csproj --nologo</automated>
  </verify>
  <done>
    `CalculateDpsHpsStats` has no `OrderBy` and no per-category `Where`/`Sum`/`Count` LINQ passes — one `foreach (var line in state)`; crit% divides by `state.Count`; damage/heal are independent ifs; min/max tracked by `.TimeStamp.TimeOfDay`; `timeSpan` uses `state.Count > 1 ? ... : TimeSpan.FromSeconds(1)`; the Accumulator and the DateTime.Now filter are unchanged; all DpsHpsMathTests pass UNCHANGED; full suite (102 + Wave-0 additions) green with zero skips; `dotnet build SwtorLogParser.slnx` succeeds.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
- `SwtorLogParser/Monitor/CombatLogsMonitor.cs` — `CalculateDpsHpsStats` collapsed to a single pass (no `OrderBy`, no multi-pass LINQ); `Accumulator` and the `DateTime.Now` filter unchanged; identical DPS/HPS/crit%/window output.
- `SwtorLogParser.Tests/DpsHpsMathTests.cs` — new `Single_Line_Uses_OneSecond_Window` edge test locking the `count<=1 ⇒ 1s` branch.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure in-memory aggregation refactor over already-parsed lines. No IO, no untrusted input crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering (output integrity) | DPS/HPS/crit% numbers | mitigate | Behavior-preserving rewrite verified by the 7-fact `DpsHpsMathTests` run on every commit; any numeric drift reverts the change. |

Local desktop tool — no network/auth/crypto/file surface. Core lib stays `IsAotCompatible=true` (no reflection added).
</threat_model>

<verification>
- `dotnet test SwtorLogParser.Tests/SwtorLogParser.Tests.csproj --nologo` → all green, zero skips; `DpsHpsMathTests` (7 facts) pass UNCHANGED.
- `dotnet build SwtorLogParser.slnx -c Debug` succeeds (core lib stays AOT-compatible).
- Grep guard: no `OrderBy` in `CalculateDpsHpsStats`; the `Accumulator` `RemoveWhere(...AddSeconds(-10))` and the `DateTime.Now.AddSeconds(-10)` filter are present and unchanged.
</verification>

<success_criteria>
- `CalculateDpsHpsStats` produces IDENTICAL DPS, HPS, crit%, and null-on-zero/infinity output via a single pass (no sort, no multi-pass LINQ).
- The 10s sliding-window semantics (Accumulator) and the DateTime.Now filter are unchanged.
- Full test suite green with zero skips after every commit; DpsHpsMathTests pass unchanged.
</success_criteria>

<output>
Create `.planning/phases/04-performance/04-03-SUMMARY.md` when done.
</output>
