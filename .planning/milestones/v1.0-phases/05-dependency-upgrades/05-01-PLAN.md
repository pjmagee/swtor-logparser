---
phase: 05-dependency-upgrades
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Directory.Packages.props
  - SwtorLogParser/SwtorLogParser.csproj
  - SwtorLogParser.Cli/SwtorLogParser.Cli.csproj
  - SwtorLogParser.Native.Cli/SwtorLogParser.Native.Cli.csproj
  - SwtorLogParser.Overlay/SwtorLogParser.Overlay.csproj
  - SwtorLogParser.Tests/SwtorLogParser.Tests.csproj
autonomous: true
requirements: [DEP-01, DEP-02]
must_haves:
  truths:
    - "Directory.Packages.props exists at solution root with ManagePackageVersionsCentrally=true"
    - "No PackageReference in any csproj carries a Version= attribute (all versions live in the props)"
    - "No package version anywhere is preview, alpha, or beta — every centrally managed version is a GA release"
    - "dotnet restore SwtorLogParser.slnx succeeds with no NU1008 error"
    - "dotnet build SwtorLogParser.slnx succeeds (core lib still builds with Logging.Abstractions resolvable)"
    - "dotnet test SwtorLogParser.Tests/SwtorLogParser.Tests.csproj is GREEN with 106 passing, zero skips"
    - "Core lib SwtorLogParser stays IsAotCompatible=true"
  artifacts:
    - path: "Directory.Packages.props"
      provides: "Central package version source for all projects"
      contains: "ManagePackageVersionsCentrally"
    - path: "SwtorLogParser/SwtorLogParser.csproj"
      provides: "Core lib with dead refs removed + explicit Logging.Abstractions"
      contains: "Microsoft.Extensions.Logging.Abstractions"
  key_links:
    - from: "every *.csproj PackageReference"
      to: "Directory.Packages.props PackageVersion"
      via: "central package management (bare Include, version resolved from props)"
      pattern: "ManagePackageVersionsCentrally"
---

<objective>
Introduce Central Package Management (DEP-02) and move every NuGet pin to a stable GA release (DEP-01). Create a root `Directory.Packages.props` holding all `<PackageVersion>` entries at the research-confirmed GA versions, strip every `Version=` attribute from every `<PackageReference>` across all five csproj (to avoid NU1008), remove the three unused core-lib provider/DI refs, and add an explicit `Microsoft.Extensions.Logging.Abstractions` reference to the core lib (which currently receives it only transitively via the removed packages).

Purpose: Eliminate all preview/alpha/beta pins and make versions centrally auditable — the foundation that the System.CommandLine removal plan (05-02) builds on. This plan touches ALL csproj, so it runs alone in Wave 1 before any per-host plan.
Output: New `Directory.Packages.props`; all five csproj converted to bare (version-less) `<PackageReference>` entries at GA versions.
</objective>

<execution_context>
@D:/Projects/pjmagee/swtor-logparser/.claude/gsd-core/workflows/execute-plan.md
@D:/Projects/pjmagee/swtor-logparser/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dependency-upgrades/05-RESEARCH.md
@.planning/phases/05-dependency-upgrades/05-PATTERNS.md
</context>

<artifacts_this_phase_produces>
- `Directory.Packages.props` (NEW, repo root) — single source of truth for all NuGet versions (this plan).
- Spectre.Console `Table` rendering in the managed CLI — produced in plan 05-02, NOT this plan.
</artifacts_this_phase_produces>

<read_first>
Before editing, read these to capture exact current content (do not guess line contents):
- `Directory.Packages.props` does NOT yet exist (confirmed) — you CREATE it.
- `SwtorLogParser/SwtorLogParser.csproj` — the core lib: 4 PackageReference entries (3 to remove, System.Reactive to keep+bump); has `<IsAotCompatible>true</IsAotCompatible>` (DO NOT remove).
- `SwtorLogParser.Cli/SwtorLogParser.Cli.csproj` — has `System.CommandLine.Rendering` ref (LEAVE the ref for plan 05-02; this plan does NOT add Spectre.Console). It carries `Version=` you must strip when CPM is on — but since this ref is being removed entirely in 05-02, see action note below.
- `SwtorLogParser.Native.Cli/SwtorLogParser.Native.Cli.csproj` — has `Microsoft.Extensions.Logging.Abstractions` (keep+bump) and `System.CommandLine` (removed in 05-02). Has `<PublishAot>true</PublishAot>` (DO NOT touch).
- `SwtorLogParser.Tests/SwtorLogParser.Tests.csproj` — 4 test PackageReference entries; `xunit.runner.visualstudio` and `coverlet.collector` carry `<IncludeAssets>`/`<PrivateAssets>` CHILD elements that MUST stay.
- `SwtorLogParser.Overlay/SwtorLogParser.Overlay.csproj` — no active PackageReference (only commented-out block); nothing to strip, but it silently participates in CPM.
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Create Directory.Packages.props with all GA versions</name>
  <files>Directory.Packages.props</files>
  <action>
Create a NEW file `Directory.Packages.props` at the repo root (sibling of `SwtorLogParser.slnx`). Root element `<Project>` (no Sdk attribute). Inside, one `<PropertyGroup>` containing `<ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>` (DEP-02), and one `<ItemGroup>` with a `<PackageVersion Include="..." Version="..." />` line for EACH of the seven managed packages at these EXACT research-confirmed GA versions (DEP-01 — verified against the NuGet flat-container at plan time; if a newer GA is confirmed at execution, use it, but never a preview/alpha/beta):

- System.Reactive = 6.0.2
- Microsoft.Extensions.Logging.Abstractions = 8.0.3
- Spectre.Console = 0.57.0   (declared here now so plan 05-02 can reference it bare; no csproj consumes it yet in this plan)
- xunit = 2.9.3
- xunit.runner.visualstudio = 3.1.5
- Microsoft.NET.Test.Sdk = 18.6.0
- coverlet.collector = 6.0.4

Do NOT include any version for System.CommandLine or System.CommandLine.Rendering — those packages are being removed entirely (plan 05-02), so they get no `<PackageVersion>` entry. Do NOT add Microsoft.Extensions.DependencyInjection / Logging.Console / Logging.Debug — those are the dead refs being removed in Task 2.
  </action>
  <verify>
    <automated>Test-Path "Directory.Packages.props" returns True AND the file contains the string "ManagePackageVersionsCentrally" AND contains "6.0.2" AND contains "0.57.0" AND contains no "-preview" / "-alpha" / "-beta" / "-pre" substrings.</automated>
  </verify>
  <done>`Directory.Packages.props` exists at repo root, declares ManagePackageVersionsCentrally=true and exactly seven GA `<PackageVersion>` entries, none preview/alpha/beta.</done>
</task>

<task type="auto">
  <name>Task 2: Convert core lib + remove dead refs + add explicit Logging.Abstractions</name>
  <files>SwtorLogParser/SwtorLogParser.csproj</files>
  <action>
Edit `SwtorLogParser/SwtorLogParser.csproj` (DEP-01, DEP-02). In the package `<ItemGroup>`:
1. DELETE the three unused refs entirely (Phase 3 WR-04 left them dead — verified zero usages of a DI container or Console/Debug providers in the core lib): `Microsoft.Extensions.DependencyInjection`, `Microsoft.Extensions.Logging.Console`, `Microsoft.Extensions.Logging.Debug`.
2. KEEP `System.Reactive` but make it BARE — change `<PackageReference Include="System.Reactive" Version="6.0.1-preview.1" />` to `<PackageReference Include="System.Reactive" />` (version now from the props).
3. ADD an explicit bare `<PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />`. CRITICAL (per 05-PATTERNS do-not-break #7 + 05-RESEARCH): the core lib uses `ILogger<T>`/`NullLogger<T>` from `Microsoft.Extensions.Logging.Abstractions`, which until now arrived TRANSITIVELY through the three removed packages. Removing them without adding this explicit ref breaks the build. The `using Microsoft.Extensions.Logging;` in `CombatLogsMonitor.cs` resolves from Abstractions, not from the providers.

Do NOT touch `<IsAotCompatible>true</IsAotCompatible>`, the `<InternalsVisibleTo>` item, or `<TargetFramework>net8.0</TargetFramework>`.
  </action>
  <verify>
    <automated>dotnet build SwtorLogParser/SwtorLogParser.csproj succeeds (exit 0). The csproj contains "Microsoft.Extensions.Logging.Abstractions" and "System.Reactive", contains NO "Microsoft.Extensions.DependencyInjection" / "Logging.Console" / "Logging.Debug", and NO Version= attribute on any PackageReference.</automated>
  </verify>
  <done>Core lib references only System.Reactive + Microsoft.Extensions.Logging.Abstractions (both bare); the three dead provider/DI refs are gone; it builds; IsAotCompatible=true retained.</done>
</task>

<task type="auto">
  <name>Task 3: Strip Version= from remaining csproj (Native CLI, Cli, Tests) and verify restore/build/test</name>
  <files>SwtorLogParser.Native.Cli/SwtorLogParser.Native.Cli.csproj, SwtorLogParser.Cli/SwtorLogParser.Cli.csproj, SwtorLogParser.Tests/SwtorLogParser.Tests.csproj</files>
  <action>
Strip the `Version=` attribute from EVERY remaining `<PackageReference>` so CPM resolves all versions (avoids NU1008 — Pitfall 1). Do NOT remove or reorder the package refs themselves in this plan (System.CommandLine removal + Spectre.Console add happen in plan 05-02); ONLY remove `Version=` attributes here.

1. `SwtorLogParser.Native.Cli/SwtorLogParser.Native.Cli.csproj`: make BOTH refs bare —
   `Microsoft.Extensions.Logging.Abstractions` (drop `Version="8.0.0-preview.5.23280.8"`) and
   `System.CommandLine` (drop `Version="2.0.0-beta4.22272.1"` — it stays referenced for now; 05-02 removes it). Do NOT touch `<PublishAot>true</PublishAot>` or `<DockerDefaultTargetOS>` (Docker removal is plan 05-02).
2. `SwtorLogParser.Cli/SwtorLogParser.Cli.csproj`: make `System.CommandLine.Rendering` bare (drop `Version="0.4.0-alpha.22272.1"`). HOWEVER — this package has NO `<PackageVersion>` entry in the props (it is being removed in 05-02 and never gets a GA version). A bare reference with no matching `<PackageVersion>` causes a restore error (NU1010). To keep this plan's restore green WITHOUT pre-doing 05-02's work, leave the `System.CommandLine.Rendering` reference with its current `Version=` attribute INTACT (CPM tolerates a pinned version on a package that has no `<PackageVersion>` entry — the NU1008 rule only fires when BOTH a `<PackageVersion>` and a `Version=` exist; here there is no `<PackageVersion>`). Add a code comment `<!-- removed in plan 05-02 -->` next to it. (Net effect: Cli.csproj is unchanged in this plan; 05-02 deletes the ref outright.)
3. `SwtorLogParser.Tests/SwtorLogParser.Tests.csproj`: make all four refs bare — drop `Version=` from `Microsoft.NET.Test.Sdk`, `xunit`, `xunit.runner.visualstudio`, `coverlet.collector`. CRITICAL (do-not-break #8): KEEP the `<IncludeAssets>` and `<PrivateAssets>` CHILD elements on `xunit.runner.visualstudio` and `coverlet.collector` — CPM moves ONLY the version, never the asset metadata. These two refs stay as multi-line `<PackageReference>...</PackageReference>` blocks with their children; just remove the `Version=` attribute from the opening tag.

After editing, run the gates: `dotnet restore SwtorLogParser.slnx`, then `dotnet build SwtorLogParser.slnx`, then `dotnet test SwtorLogParser.Tests/SwtorLogParser.Tests.csproj`. If restore fails with NU1010 (missing PackageVersion) or NU1008 (version on reference), fix the offending csproj and re-run. (If a stale `obj/` masks the new resolution, the build gate covers it — clean `obj/` and re-restore if needed; see 05-RESEARCH Runtime State Inventory.)
  </action>
  <verify>
    <automated>dotnet restore SwtorLogParser.slnx succeeds (no NU1008/NU1010); dotnet build SwtorLogParser.slnx succeeds; dotnet test SwtorLogParser.Tests/SwtorLogParser.Tests.csproj exits 0 with 106 passing and zero skips. No managed-package PackageReference (System.Reactive, Logging.Abstractions, xunit, xunit.runner.visualstudio, Microsoft.NET.Test.Sdk, coverlet.collector) carries a Version= attribute. The two test refs still carry their IncludeAssets/PrivateAssets children.</automated>
  </verify>
  <done>All managed PackageReference entries are version-less and resolve from the props; restore is NU1008/NU1010-clean; full solution builds; 106 tests green, zero skips; test asset metadata preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NuGet restore → build graph | External package versions enter the build; supply-chain surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-SC | Tampering | NuGet GA package resolution (System.Reactive, Logging.Abstractions, xunit*, Test.Sdk, coverlet, Spectre.Console version declared) | mitigate | Use only research-verified GA versions from the official nuget.org feed (Package Legitimacy Audit in 05-RESEARCH: all 1st-party or high-reputation, named maintainers, official repos). No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages — all NuGet IDs verified against the registry, so no blocking human-verify checkpoint is required. Spectre.Console (the one new ID) is verified (44.6M downloads, official spectreconsole repo). |
</threat_model>

<verification>
- `dotnet restore SwtorLogParser.slnx` succeeds with GA feeds only — no NU1008, no NU1010, no preview/alpha/beta resolved.
- `dotnet build SwtorLogParser.slnx` succeeds.
- `dotnet test SwtorLogParser.Tests/SwtorLogParser.Tests.csproj` → 106 passing, zero skips (behavior preserved across the GA bump).
- Grep confirmation: no `Version=` on any managed-package `<PackageReference>`; no `-preview`/`-alpha`/`-beta`/`-pre` anywhere in `Directory.Packages.props`.
- Core lib retains `<IsAotCompatible>true</IsAotCompatible>`.
</verification>

<success_criteria>
- ROADMAP Phase 5 criterion 1 satisfied: `Directory.Packages.props` exists at solution root; managed packages carry no per-csproj version attribute.
- ROADMAP Phase 5 criterion 2 satisfied: no package pinned to preview/alpha/beta; `dotnet restore` succeeds.
- DEP-01 + DEP-02 implemented; dead core-lib refs removed; explicit Logging.Abstractions added so the core lib still builds.
- 106 tests green, zero skips; solution builds; core lib stays AOT-compatible.
</success_criteria>

<output>
Create `.planning/phases/05-dependency-upgrades/05-01-SUMMARY.md` when done.
</output>
