---
phase: 06-dotnet-10-upgrade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Directory.Packages.props
  - SwtorLogParser/SwtorLogParser.csproj
  - SwtorLogParser.Cli/SwtorLogParser.Cli.csproj
  - SwtorLogParser.Native.Cli/SwtorLogParser.Native.Cli.csproj
  - SwtorLogParser.Overlay/SwtorLogParser.Overlay.csproj
  - SwtorLogParser.Tests/SwtorLogParser.Tests.csproj
autonomous: true
requirements: [PLAT-01]

must_haves:
  truths:
    - "Every *.csproj targets net10.0 (or net10.0-windows for the Overlay) — no net8.0 TargetFramework remains anywhere"
    - "Microsoft.Extensions.Logging.Abstractions is pinned to 10.0.9 GA; no preview/alpha/beta version exists in Directory.Packages.props"
    - "dotnet restore + dotnet build SwtorLogParser.slnx -c Release succeed on net10.0 with no NU1605/NU1008"
    - "dotnet test runs all 106 existing tests green with zero skips on the net10.0 test host"
    - "Native AOT CLI publish produces zero IL2xxx/IL3xxx code-gen warnings; core lib stays IsAotCompatible=true and Native CLI stays PublishAot=true"
  artifacts:
    - path: "Directory.Packages.props"
      provides: "Microsoft.Extensions.Logging.Abstractions pinned to 10.0.9; all other versions unchanged"
      contains: "10.0.9"
    - path: "SwtorLogParser/SwtorLogParser.csproj"
      provides: "Core library targeting net10.0 with IsAotCompatible=true retained"
      contains: "net10.0"
    - path: "SwtorLogParser.Cli/SwtorLogParser.Cli.csproj"
      provides: "Managed CLI targeting net10.0 with LangVersion=preview removed"
      contains: "net10.0"
    - path: "SwtorLogParser.Native.Cli/SwtorLogParser.Native.Cli.csproj"
      provides: "Native AOT CLI targeting net10.0 with PublishAot=true retained"
      contains: "net10.0"
    - path: "SwtorLogParser.Overlay/SwtorLogParser.Overlay.csproj"
      provides: "WinForms overlay targeting net10.0-windows with UseWindowsForms retained"
      contains: "net10.0-windows"
    - path: "SwtorLogParser.Tests/SwtorLogParser.Tests.csproj"
      provides: "Test project targeting net10.0"
      contains: "net10.0"
  key_links:
    - from: "all 5 *.csproj TargetFramework"
      to: "installed .NET 10 SDK"
      via: "MSBuild TFM resolution (net10.0 / net10.0-windows)"
      pattern: "net10\\.0"
    - from: "SwtorLogParser.csproj + SwtorLogParser.Native.Cli.csproj PackageReference"
      to: "Directory.Packages.props Microsoft.Extensions.Logging.Abstractions 10.0.9"
      via: "central package management version resolution"
      pattern: "Microsoft\\.Extensions\\.Logging\\.Abstractions.*10\\.0\\.9"
---

<objective>
Upgrade every project in the solution from .NET 8 to .NET 10 (LTS) and re-verify the full build/test/AOT chain. This is a purely mechanical, behavior-preserving upgrade (PLAT-01, closes issue #1): five `<TargetFramework>` string edits, one central package version bump, and the optional removal of the now-redundant `LangVersion=preview`. NO code or behavior changes — the 106 existing tests ARE the regression contract.

Purpose: Move the codebase onto the current LTS runtime before Phase 7 stands up CI, so the pipeline targets a single .NET 10 SDK. The dev machine already runs SDK 10.0.301 + NETCore.App/WindowsDesktop.App 10.0.9, so net10.0 and net10.0-windows build natively today.

Output: All 5 `.csproj` on net10.0 / net10.0-windows; `Microsoft.Extensions.Logging.Abstractions` on 10.0.9 GA; restore + Release build green; 106 tests green with zero skips; Native AOT code-gen IL-warning-free.

## Artifacts this phase produces
- `Directory.Packages.props` — `Microsoft.Extensions.Logging.Abstractions` bumped 8.0.3 → 10.0.9 (all other versions byte-for-byte unchanged)
- `SwtorLogParser/SwtorLogParser.csproj` — `net8.0` → `net10.0` (IsAotCompatible=true retained)
- `SwtorLogParser.Cli/SwtorLogParser.Cli.csproj` — `net8.0` → `net10.0`, `<LangVersion>preview</LangVersion>` removed (C# 14 default)
- `SwtorLogParser.Native.Cli/SwtorLogParser.Native.Cli.csproj` — `net8.0` → `net10.0` (PublishAot=true retained)
- `SwtorLogParser.Overlay/SwtorLogParser.Overlay.csproj` — `net8.0-windows` → `net10.0-windows` (UseWindowsForms retained)
- `SwtorLogParser.Tests/SwtorLogParser.Tests.csproj` — `net8.0` → `net10.0`
</objective>

<execution_context>
@D:/Projects/pjmagee/swtor-logparser/.claude/gsd-core/workflows/execute-plan.md
@D:/Projects/pjmagee/swtor-logparser/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dotnet-10-upgrade/06-CONTEXT.md
@.planning/phases/06-dotnet-10-upgrade/06-RESEARCH.md
@.planning/phases/06-dotnet-10-upgrade/06-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump 5 TFMs to net10.0 + Logging.Abstractions to 10.0.9 + drop LangVersion=preview</name>
  <read_first>
    - Directory.Packages.props
    - SwtorLogParser/SwtorLogParser.csproj
    - SwtorLogParser.Cli/SwtorLogParser.Cli.csproj
    - SwtorLogParser.Native.Cli/SwtorLogParser.Native.Cli.csproj
    - SwtorLogParser.Overlay/SwtorLogParser.Overlay.csproj
    - SwtorLogParser.Tests/SwtorLogParser.Tests.csproj
  </read_first>
  <files>Directory.Packages.props, SwtorLogParser/SwtorLogParser.csproj, SwtorLogParser.Cli/SwtorLogParser.Cli.csproj, SwtorLogParser.Native.Cli/SwtorLogParser.Native.Cli.csproj, SwtorLogParser.Overlay/SwtorLogParser.Overlay.csproj, SwtorLogParser.Tests/SwtorLogParser.Tests.csproj</files>
  <action>
    Apply the EXACT mechanical edits below — these are the entire change (per D-decisions: locked target frameworks + locked package decision in 06-CONTEXT.md, verified by 06-RESEARCH.md Pattern 1 + Pattern 2). Make NO other edits to any file. Do NOT touch System.Reactive (6.0.2), xunit (2.9.3), xunit.runner.visualstudio (3.1.5), Microsoft.NET.Test.Sdk (18.6.0), coverlet.collector (6.0.4), or Spectre.Console (0.57.0) — research VERIFIED all six are net10-compatible GA and the locked decision is to keep them. Do NOT migrate xunit 2.x to v3 (explicitly out of scope). Do NOT add any `Version=` attribute to any `<PackageReference>` (CPM is active — that triggers NU1008). Do NOT add a global.json.

    1. `Directory.Packages.props`: change the single line `<PackageVersion Include="Microsoft.Extensions.Logging.Abstractions" Version="8.0.3" />` to `<PackageVersion Include="Microsoft.Extensions.Logging.Abstractions" Version="10.0.9" />`. Leave all six other `<PackageVersion>` lines byte-for-byte unchanged. (10.0.9 is the current GA patch matching the installed 10.0.9 runtime and prevents the NU1605 transitive-downgrade risk described in Pitfall 3.)

    2. `SwtorLogParser/SwtorLogParser.csproj`: change `<TargetFramework>net8.0</TargetFramework>` to `<TargetFramework>net10.0</TargetFramework>`. Keep `<IsAotCompatible>true</IsAotCompatible>` exactly as-is.

    3. `SwtorLogParser.Cli/SwtorLogParser.Cli.csproj`: change `<TargetFramework>net8.0</TargetFramework>` to `<TargetFramework>net10.0</TargetFramework>`, AND delete the entire `<LangVersion>preview</LangVersion>` line (net10 defaults to C# 14, a superset of preview-12 — per Claude's Discretion in 06-CONTEXT.md, recommended by research). Leave `<PublishAot>false</PublishAot>` unchanged.

    4. `SwtorLogParser.Native.Cli/SwtorLogParser.Native.Cli.csproj`: change `<TargetFramework>net8.0</TargetFramework>` to `<TargetFramework>net10.0</TargetFramework>`. Keep `<PublishAot>true</PublishAot>` exactly as-is.

    5. `SwtorLogParser.Overlay/SwtorLogParser.Overlay.csproj`: change `<TargetFramework>net8.0-windows</TargetFramework>` to `<TargetFramework>net10.0-windows</TargetFramework>`. Keep `<UseWindowsForms>true</UseWindowsForms>` exactly as-is.

    6. `SwtorLogParser.Tests/SwtorLogParser.Tests.csproj`: change `<TargetFramework>net8.0</TargetFramework>` to `<TargetFramework>net10.0</TargetFramework>`.

    After all six edits, run `dotnet clean SwtorLogParser.slnx` to purge stale net8.0 bin/obj output (Pitfall 1: incremental MSBuild keys off timestamps, not TFM identity, so a stale obj/ can mix net8 IL into the net10 build/AOT publish). Then run `dotnet restore SwtorLogParser.slnx` and `dotnet build SwtorLogParser.slnx -c Release`. If restore prints NU1901-1904 transitive advisories, record them in the SUMMARY as informational (net10 NuGetAudit now audits transitive packages) — they are not a hard failure unless a CVE warrants review. If restore fails with NU1605 naming a Microsoft.Extensions.* package, the Logging.Abstractions pin is wrong — re-confirm it reads 10.0.9. If build fails with NU1008, a stray `Version=` was introduced on a PackageReference — remove it.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if (Select-String -Path (Get-ChildItem -Recurse -Filter *.csproj | Select-Object -ExpandProperty FullName) -Pattern 'net8\.0' -Quiet) { Write-Error 'net8.0 still present in a csproj'; exit 1 }; if (-not (Select-String -Path Directory.Packages.props -Pattern 'Microsoft\.Extensions\.Logging\.Abstractions.*10\.0\.9' -Quiet)) { Write-Error 'Logging.Abstractions not at 10.0.9'; exit 1 }; if (Select-String -Path Directory.Packages.props -Pattern '(?i)-(preview|alpha|beta|rc|pre)' -Quiet) { Write-Error 'preview/alpha/beta version present'; exit 1 }; Write-Host 'csproj/CPM assertions OK'"</automated>
    <automated>dotnet restore SwtorLogParser.slnx</automated>
    <automated>dotnet build SwtorLogParser.slnx -c Release</automated>
  </verify>
  <acceptance_criteria>
    - No `net8.0` (or `net8.0-windows`) substring appears in ANY `*.csproj` (grep returns zero matches)
    - All 5 csproj TargetFramework values are exactly `net10.0` (4 projects) or `net10.0-windows` (Overlay)
    - `Directory.Packages.props` contains `Microsoft.Extensions.Logging.Abstractions` at `Version="10.0.9"`
    - No package version anywhere is preview/alpha/beta/rc/pre
    - `SwtorLogParser.csproj` still has `<IsAotCompatible>true</IsAotCompatible>`; `SwtorLogParser.Native.Cli.csproj` still has `<PublishAot>true</PublishAot>`; `SwtorLogParser.Overlay.csproj` still has `<UseWindowsForms>true</UseWindowsForms>`
    - `<LangVersion>preview</LangVersion>` is absent from `SwtorLogParser.Cli.csproj`
    - `dotnet restore SwtorLogParser.slnx` exits 0 (no NU1605 / NU1008)
    - `dotnet build SwtorLogParser.slnx -c Release` exits 0 (all 5 projects compile on net10.0)
  </acceptance_criteria>
  <done>All 5 projects target net10.0/net10.0-windows, Logging.Abstractions is 10.0.9, no preview packages remain, and restore + Release build succeed on .NET 10.</done>
</task>

<task type="auto">
  <name>Task 2: Re-verify the 106-test regression suite is green on net10.0</name>
  <read_first>
    - SwtorLogParser.Tests/SwtorLogParser.Tests.csproj
    - .planning/phases/06-dotnet-10-upgrade/06-VALIDATION.md
  </read_first>
  <files>(no edits — verification only)</files>
  <action>
    Run the full existing test suite on the net10.0 test host to prove the TFM upgrade is behavior-preserving (PLAT-01 success criterion 3). The suite is hermetic (Phase 3 `ICombatLogSource` seam) and deterministic (`[Collection]`), so no fixture changes are needed. Run `dotnet test SwtorLogParser.Tests/SwtorLogParser.Tests.csproj -c Release`. Confirm the run reports 106 passed, 0 failed, 0 skipped. If any test fails, this is a genuine behavior regression introduced by the runtime upgrade (not expected per research) — STOP, do not "fix" tests to make them pass; record the failure and surface it. Do NOT add, modify, skip, or delete any test. If the total is not exactly 106, record the actual count in the SUMMARY (baseline is the 106 confirmed across Phases 1-4) and confirm zero skips regardless.
  </action>
  <verify>
    <automated>dotnet test SwtorLogParser.Tests/SwtorLogParser.Tests.csproj -c Release</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test SwtorLogParser.Tests/SwtorLogParser.Tests.csproj -c Release` exits 0
    - Test summary reports 0 failed and 0 skipped
    - Passing count is 106 (the established baseline); any deviation is recorded in the SUMMARY
    - No test file was added, modified, skipped, or deleted to achieve green
  </acceptance_criteria>
  <done>The full existing suite runs green on the net10.0 test host with zero skips, proving the upgrade preserved behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Re-verify Native AOT code-gen is IL-warning-clean on net10.0</name>
  <read_first>
    - SwtorLogParser.Native.Cli/SwtorLogParser.Native.Cli.csproj
    - SwtorLogParser/SwtorLogParser.csproj
    - .planning/phases/06-dotnet-10-upgrade/06-RESEARCH.md
  </read_first>
  <files>(no edits — verification only)</files>
  <action>
    Run `dotnet publish SwtorLogParser.Native.Cli -c Release` to exercise the Native AOT code-gen path on net10.0 (PLAT-01 success criterion 4). The in-scope gate is ZERO IL2xxx and IL3xxx trim/AOT warnings from the ILCompiler analysis stage (the managed -> IL -> code-gen stage that runs BEFORE the native link). The core library is reflection-free and `IsAotCompatible=true`, so research expects zero warnings.

    IMPORTANT — distinguish two failure modes (Pitfall 4): (a) any `IL2xxx`/`IL3xxx` warning in publish output is an in-scope AOT regression and FAILS this task; (b) a failure at the final native link step (`link.exe` / `lld` not found, MSVC toolchain absent) is a KNOWN LOCAL ENV GAP, not an AOT-cleanliness failure — record it in the SUMMARY as "native link unavailable locally; Phase 7 CI on windows-latest exercises the full link" and treat the task as passing on the code-gen criterion. Capture the publish output and explicitly state in the SUMMARY whether any IL warning appeared (expected: none) and whether the run reached/passed or stopped at the native link step.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$out = dotnet publish SwtorLogParser.Native.Cli -c Release 2>&1 | Out-String; Write-Host $out; if ($out -match 'IL2\d{3}' -or $out -match 'IL3\d{3}') { Write-Error 'IL2xxx/IL3xxx AOT warning present'; exit 1 }; Write-Host 'AOT code-gen is IL-warning-clean (native link step may be env-gated)'; exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet publish SwtorLogParser.Native.Cli -c Release` output contains ZERO `IL2xxx` and ZERO `IL3xxx` warnings
    - `SwtorLogParser.Native.Cli.csproj` retains `<PublishAot>true</PublishAot>` and `SwtorLogParser.csproj` retains `<IsAotCompatible>true</IsAotCompatible>`
    - The SUMMARY records whether the native link step completed locally or was env-gated (missing MSVC linker → deferred to Phase 7 CI, acceptable)
  </acceptance_criteria>
  <done>The Native AOT publish produces no IL2xxx/IL3xxx warnings on net10.0; AOT compatibility is preserved. A missing local native linker is recorded as a known env gap, not a failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NuGet restore → build | Package versions resolved from the public NuGet feed under central package management; net10 restore now audits transitive packages (NuGetAudit) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering / EoP | Transitive dependency pulled by net10 restore resolution | mitigate | net10 `dotnet restore` runs transitive NuGetAudit (NU1901-1904); record any advisory it prints in the SUMMARY and review CVE severity. No new direct packages are added this phase. |
| T-06-SC | Tampering | NuGet package version bump (Logging.Abstractions 8.0.3 → 10.0.9) | accept | Not a new/unknown package — it is an existing first-party Microsoft dependency already in Directory.Packages.props, version-verified against the authoritative NuGet flat-container API in 06-RESEARCH.md Package Legitimacy Audit (verdict OK, no [ASSUMED]/[SUS]/[SLOP]). No npm/pip/cargo installs occur. No legitimacy checkpoint required. |
| T-06-02 | Information disclosure | Local desktop log reader | accept | No network/auth/PII surface; files opened read-only (BUG-07, unchanged); this phase changes no parsing or IO code. |
</threat_model>

<verification>
Phase-level gates (run after all tasks, before `/gsd-verify-work`):

1. `dotnet restore SwtorLogParser.slnx` — resolves cleanly on net10, no NU1605/NU1008.
2. `dotnet build SwtorLogParser.slnx -c Release` — all 5 projects compile on net10.0.
3. `dotnet test SwtorLogParser.Tests/SwtorLogParser.Tests.csproj -c Release` — 106 green, 0 skips.
4. `dotnet publish SwtorLogParser.Native.Cli -c Release` — zero IL2xxx/IL3xxx code-gen warnings (native link step may be env-gated locally → Phase 7 CI).
5. Grep assertion: no `net8.0` in any `*.csproj`; `Microsoft.Extensions.Logging.Abstractions` at `10.0.9`; no preview/alpha/beta anywhere.
</verification>

<success_criteria>
- [ ] Every `*.csproj` targets `net10.0` (or `net10.0-windows` for the Overlay) — no `net8.0` TargetFramework remains
- [ ] `Microsoft.Extensions.Logging.Abstractions` is `10.0.9` in `Directory.Packages.props`; no preview/alpha/beta package anywhere; `dotnet restore`/`dotnet build SwtorLogParser.slnx -c Release` succeed
- [ ] `dotnet test` is green — 106 tests, zero skips — on the net10.0 test host
- [ ] Native AOT CLI publish is IL2xxx/IL3xxx-clean on net10.0; core lib stays `IsAotCompatible=true`, Native CLI stays `PublishAot=true`
- [ ] `LangVersion=preview` removed from `SwtorLogParser.Cli.csproj` (C# 14 default)
</success_criteria>

<output>
Create `.planning/phases/06-dotnet-10-upgrade/06-01-SUMMARY.md` when done. Record: the exact version/TFM edits applied, the result of all four gates (restore / build / test 106 / AOT publish), any NU1901-1904 transitive advisories printed by restore (informational), and whether the Native AOT native link step ran locally or was deferred to Phase 7 CI (missing MSVC linker = known env gap, acceptable).
</output>
