---
phase: 01-parser-safety-net
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - SwtorLogParser.Tests/AbilityTests.cs
  - SwtorLogParser.Tests/ActionTests.cs
autonomous: true
requirements: [TEST-03]

must_haves:
  truths:
    - "Ability.Parse (Ability.cs:10-21) does NOT read .Id, so Ability.Parse with non-numeric brace content returns a non-null Ability (LAZY); .Id throws on access via the inherited GetId (GameObject.cs:79-99)"
    - "Ability golden [Fact] locks current correct Name + unsigned Id output"
    - "Action.Parse (Action.cs try/catch) returns null gracefully when a child GameObject parse throws — asserted green today via a [Theory] of malformed inner fragments"
    - "Action golden [Fact]s lock event/effect (and nested parent) field output"
    - "dotnet test runs fully GREEN with zero skips"
  artifacts:
    - path: "SwtorLogParser.Tests/AbilityTests.cs"
      provides: "LAZY inherited-Id throw characterization, golden lock"
      contains: "Assert.Throws"
    - path: "SwtorLogParser.Tests/ActionTests.cs"
      provides: "Graceful-null [Theory] for malformed inner fragments, golden locks"
      contains: "Assert.Null"
  key_links:
    - from: "SwtorLogParser.Tests/AbilityTests.cs"
      to: "Ability.Parse + inherited .Id getter"
      via: "static factory then .Id access"
      pattern: "Ability\\.Parse\\("
    - from: "SwtorLogParser.Tests/ActionTests.cs"
      to: "Action.Parse"
      via: "static factory call on .AsMemory() input"
      pattern: "Action\\.Parse\\("
---

<objective>
Characterize the two remaining models — `Ability` (inherits `GameObject` but its `Parse` does NOT touch `.Id`, so it is LAZY) and `Action` (whose `Parse` wraps construction in try/catch, so it is already graceful and returns null on a throwing child). Per the verified eager/lazy map (RESEARCH.md): `Ability.Parse("Name {abc}")` returns a non-null object and only throws on `.Id` access (inherited `GetId`, GameObject.cs:79-99); `Action.Parse` catches a throwing child `GameObject` and returns null TODAY. This plan adds the LAZY `.Id` throw characterization for Ability, a graceful-null `[Theory]` for Action, and golden-line locks for both (TEST-03).

Purpose: Completes the seven-model coverage required by CONTEXT.md ("edge-case tests cover ALL Parse-factory models"). Ability's `.Id` throw is the Phase-2 BUG-05 invert site; Action is already green and needs only regression locks.
Output: Extended `AbilityTests.cs` and `ActionTests.cs` — new tests only, no production change.
</objective>

<execution_context>
@D:/Projects/pjmagee/swtor-logparser/.claude/gsd-core/workflows/execute-plan.md
@D:/Projects/pjmagee/swtor-logparser/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-parser-safety-net/01-RESEARCH.md
@.planning/phases/01-parser-safety-net/01-PATTERNS.md
@.planning/codebase/TESTING.md

# Models under test (read to confirm Ability inherits GetId and Action's try/catch)
@SwtorLogParser/Model/Ability.cs
@SwtorLogParser/Model/GameObject.cs
@SwtorLogParser/Model/Action.cs

# Target test files (extend in place — copy the existing [Fact] shape)
@SwtorLogParser.Tests/AbilityTests.cs
@SwtorLogParser.Tests/ActionTests.cs
@SwtorLogParser.Tests/GlobalUsings.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ability — golden lock and LAZY inherited-Id throw characterization</name>
  <files>SwtorLogParser.Tests/AbilityTests.cs</files>
  <read_first>
    - SwtorLogParser.Tests/AbilityTests.cs (existing analogs: Ability_With_Name_And_Id_Parsed :7-16 with unsigned id literal `3039943492370432u`, Ability_With_No_Name_And_Id_And_Nested_Parsed :18-27 — copy this shape)
    - SwtorLogParser/Model/Ability.cs (confirm: Parse :10-21 returns new Ability for any non-empty rom AFTER a cache check — it does NOT read .Id, so it is LAZY; note it writes to the shared GameObjectCache keyed on Rom.GetHashCode)
    - SwtorLogParser/Model/GameObject.cs (confirm Ability inherits GetId :79-99 → ulong.Parse on brace content; non-numeric content throws FormatException on .Id access)
    - .planning/phases/01-parser-safety-net/01-PATTERNS.md (Pattern A golden, Pattern E LAZY-throw)
  </read_first>
  <action>
    Extend AbilityTests.cs (do NOT remove the two existing [Fact]s). Snake_Case, `.AsMemory()` inside bodies:

    1. Golden lock (Pattern A) — optional reinforcement. The two existing goldens are already strong locks. Add ONE more golden `[Fact]` `Ability_Golden_Name_And_Id` only if it covers a distinct shape (e.g. a name containing an apostrophe or spaces) with a DISTINCT id from the existing tests; assert `Assert.NotNull`, `Assert.Equal("<name>", ability.Name)`, `Assert.Equal(<id>u, ability.Id)` (unsigned `u` suffix). If it would duplicate an existing case, skip this sub-item — do not add redundant goldens.

    2. LAZY inherited-Id throw (Pattern E, BUG-05). Add `Ability_NonNumeric_Id_Throws_On_Access_Today`: `var ability = Ability.Parse("Widget {abc}".AsMemory());` then `Assert.NotNull(ability);` (Ability.Parse does NOT read .Id → returns non-null) then `Assert.Throws<FormatException>(() => _ = ability.Id);` (inherited GetId → ulong.Parse). Inline comment: BUG-05, "Ability.Parse is LAZY (unlike GameObject.Parse which reads .Id eagerly); Phase 2 → graceful." CRITICAL — cache hygiene: GameObject/Ability cache on Rom.GetHashCode and never clear between tests. Use a literal UNIQUE to this test (e.g. an unusual name token) so a cached GameObject from another test/file cannot be returned and mask the parse path. If a first run does not throw, confirm the literal is not colliding with a cached entry; vary the name until `.Id` access throws as characterized, then assert the OBSERVED behavior.

    Do NOT modify Ability.cs / GameObject.cs or any production file. Built-in `Assert` only.
  </action>
  <verify>
    <automated>dotnet test SwtorLogParser.Tests/SwtorLogParser.Tests.csproj --filter "FullyQualifiedName~AbilityTests"</automated>
  </verify>
  <done>
    AbilityTests.cs contains a LAZY `.Id` throw characterization asserting `Assert.NotNull` on Parse then a throw on `.Id` access (the OBSERVED current behavior), using a cache-unique literal. Filtered `dotnet test` exits 0, zero failures, zero skips. The two pre-existing [Fact]s still pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Action — graceful-null [Theory] for malformed inner fragments and golden locks</name>
  <files>SwtorLogParser.Tests/ActionTests.cs</files>
  <read_first>
    - SwtorLogParser.Tests/ActionTests.cs (existing analogs: Event_And_Effect_Parsed :7-20, Event_And_Effect_Nested_Parsed :22-41 with ParentName/ParentId, Same_Actions_Are_Equal :43-50. Note the file alias `using Action = SwtorLogParser.Model.Action;` at line 1 — Action collides with System.Action, keep using it)
    - SwtorLogParser/Model/Action.cs (confirm: Parse wraps construction in try/catch; when a child GameObject.Parse throws — e.g. a non-numeric inner id {abc} which is EAGER in GameObject.Parse — the exception is CAUGHT and Action.Parse returns null, writing to Console.Error. So Assert.Null works green TODAY for malformed inner fragments)
    - .planning/phases/01-parser-safety-net/01-PATTERNS.md (Pattern A golden, Pattern C graceful-null)
  </read_first>
  <action>
    Extend ActionTests.cs (do NOT remove existing tests; keep the `using Action = SwtorLogParser.Model.Action;` alias). Snake_Case, `.AsMemory()` inside bodies:

    1. Graceful-null [Theory] (Pattern C, green today). Add `Action_Malformed_Inner_Fragment_Returns_Null(string raw)` with rows whose inner GameObject is malformed such that the EAGER child GameObject.Parse throws and Action.Parse's try/catch converts it to null. Candidate rows: an event/effect with a non-numeric id brace, e.g. `"AreaEntered {abc}: Imperial Fleet {137438989504}"` and `"AreaEntered {836045448953664}: Imperial Fleet {xyz}"`. Body: `Assert.Null(Action.Parse(raw.AsMemory()));`. IMPORTANT — verify the catch path: run once; the child GameObject's non-numeric id must throw inside Action.Parse and be caught → null. If a chosen row does NOT yield null (e.g. the fragment shape avoids the throwing child), replace it with one that does, OR assert the OBSERVED result and annotate. Use a DISTINCT literal per row (caches key on Rom.GetHashCode).

    2. Golden locks (Pattern A) — optional reinforcement. The two existing goldens (flat + nested) are already strong. Add a golden only if it covers a distinct shape not already locked (e.g. a single-segment action) with distinct ids; otherwise skip — do not duplicate.

    Do NOT modify Action.cs or any production file. Built-in `Assert` only.
  </action>
  <verify>
    <automated>dotnet test SwtorLogParser.Tests/SwtorLogParser.Tests.csproj --filter "FullyQualifiedName~ActionTests"</automated>
  </verify>
  <done>
    ActionTests.cs contains a graceful-null [Theory] proving Action.Parse returns null (via its try/catch) on a malformed inner fragment — asserting the OBSERVED current behavior. The file alias is retained. Filtered `dotnet test` exits 0, zero failures, zero skips. The three pre-existing tests still pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Test-only phase. No I/O beyond the in-process test runner, no network, no auth, no secrets. Inputs are in-memory string literals authored in the test bodies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | (n/a) | SwtorLogParser.Tests additions | accept | No attack surface — test-only plan adding pure in-memory parser characterization tests. Action's graceful-null tests confirm an existing V5 mitigation (try/catch → null); Ability's LAZY-Id test documents a V5 gap for Phase 2 (BUG-05). No production code, I/O, network, or secrets introduced. |
</threat_model>

<verification>
- `dotnet test SwtorLogParser.Tests/SwtorLogParser.Tests.csproj` exits 0 (full suite GREEN).
- Zero skipped tests (Assert.Throws / Assert.Null strategy — no `[Fact(Skip=...)]`).
- `git diff --stat` shows only AbilityTests.cs and ActionTests.cs changed — no `SwtorLogParser/Model/*.cs` production change.
</verification>

<success_criteria>
- Ability LAZY inherited-Id throw characterized on `.Id` access (Parse returns non-null), using a cache-unique literal (avoids Pitfall 2).
- Action graceful-null behavior locked via [Theory] of malformed inner fragments (green today via try/catch).
- Golden-line [Fact]s preserved / reinforced for both models (no redundant duplicates).
- All seven Parse-factory models now have edge-case + golden coverage across plans 01/02/03 (CONTEXT.md requirement).
- `dotnet test` GREEN with zero skips (ROADMAP success criterion 4).
- No production code modified.
</success_criteria>

<output>
Create `.planning/phases/01-parser-safety-net/01-03-SUMMARY.md` when done.

## Artifacts this phase produces (this plan)
New / extended test files:
- `SwtorLogParser.Tests/AbilityTests.cs`
- `SwtorLogParser.Tests/ActionTests.cs`

New test methods (names; finalize exact rows at execution):
- `AbilityTests.Ability_NonNumeric_Id_Throws_On_Access_Today` ([Fact], Assert.Throws<FormatException> on .Id; cache-unique literal)
- `AbilityTests.Ability_Golden_Name_And_Id` ([Fact]; only if it covers a distinct shape)
- `ActionTests.Action_Malformed_Inner_Fragment_Returns_Null` ([Theory], Assert.Null via try/catch)
</output>
